# StewardHouse — Project Manifest

Operating context for Claude Code in this repository. Read at the start of every
session. Update as locked decisions change.

---

## 1. Project

**StewardHouse** is a philanthropic planning and education platform for athletes,
their advisors, and athletic departments. The platform is **structural, never
advisory or fiduciary** — it organizes what an advisor or funder decides; it does
not decide for them. Phase 1 scope is **athletes only** (no music / entertainment
/ creator language in user-facing copy).

The core concept is **bilateral transparency** — a two-sided transparency layer
between individual funder and nonprofit, expressed through the **Giving
Partnership Profile (GPP)** (two narrative layers: Giving Style + Giving Identity;
no scores, no grades).

This build is a prototype / stress-test tool, not production. Quality over speed.
Stress-test before moving forward; build it right once.

**Four user-facing surfaces** under `src/surfaces/` (plus a public landing):
- **`individual/`** — funder-facing experience (paused at v0.6.1)
- **`advisor/`** — the advisor surface ("Philanthropic Advisor" in customer copy)
- **`enterprise/`** — institutions (athletic departments, Phase 1)
- **`operations/`** — internal StewardHouse staff view (Overview redesigned 2026)
- **`landing/`** — public entry

---

## 2. Stack & commands

- **React 18 + Vite + react-router-dom**. Plain `.jsx`, no TypeScript.
- **Inline styles over CSS custom properties** — every color, spacing, radius,
  type-size token lives in `src/styles/tokens.css` as `--sh-*`. Use
  `var(--sh-*)` in component styles. **No hex literals in components.**
- **Fixture-driven**, no backend. Source fixtures live in `src/data/`; the
  unified data layer (see §4) reads them via adapters and exposes a query API.
- **Auto-deploys to Cloudflare Pages on push to `main`.**

Commands:
- `npm run dev` — Vite dev server (auto-picks a port if 5173 is taken)
- `npm run build` — production build; the only consistent warning is the
  pre-existing chunk-size note for the single-bundle output
- **Node data verification pattern** (used in slice-verify scripts):
  ```sh
  node --input-type=module -e "import('./src/data/unified/index.js').then(({default: u}) => { ... })"
  ```
  Temp `.mjs` verify scripts at the project root are conventional for slice
  verification; remove after the slice banks.

---

## 3. Repo map

```
src/
├── App.jsx                         # top-level routes (5 surfaces)
├── main.jsx                        # React entry + providers
├── styles/
│   ├── global.css                  # h-reset, focus-visible (incl. [role="button"])
│   └── tokens.css                  # all --sh-* design tokens
├── components/                     # shared across surfaces
│   ├── Card.jsx                    # accepts as / aria-labelledby (a11y bundle 1)
│   ├── SectionLabel.jsx            # heading element with level + id props
│   ├── Chrome.jsx                  # surface chrome (nav, persona, contacts)
│   ├── AthleteProfile.jsx, UserProfile.jsx, WorkshopDetail.jsx, DailyBrief.jsx,
│   ├── ExclusionDetail.jsx, ContactsDirectory.jsx, FilteredAthletesModal.jsx,
│   ├── Modal.jsx, Button.jsx, Tag.jsx, HelpIcon.jsx, BackLink.jsx,
│   ├── Icon.jsx,                   # shared SVG-icon registry (chevron-right/left, plus, close; seed: BackLink chevron idiom; advisor bundles 4 + 6)
│   ├── useDialogA11y.jsx,          # shared dialog-a11y hook (focus trap + initial focus + Escape + trigger-restore; reuses ModalStackContext; .contains()-aware boundary; advisor bundle 6)
│   ├── BarChart.jsx, ComposeMessage.jsx, StatTile.jsx, SHLogo.jsx,
│   └── WorkshopCalendar.jsx
├── surfaces/
│   ├── landing/Landing.jsx
│   ├── individual/                 # 15 .jsx files (paused at v0.6.1)
│   ├── advisor/                    # 16 .jsx files — 8-section IA, audited 2026-06; QA arc complete (bundles 1–12 banked, tail triage included)
│   ├── enterprise/                 # 7 surface files + reports/, setup/, shared/
│   └── operations/
│       ├── OperationsSurface.jsx              # Overview + route shell + 5 directory routes
│       └── directories/
│           ├── IndividualsDirectory.jsx       # 77 records · 4-source chips · URL-state
│           ├── InstitutionsDirectory.jsx      # 4 records · partner-practice FK · URL-state
│           ├── AdvisorPracticesDirectory.jsx  # 7 records · lead-advisor FK · URL-state
│           ├── OrganizationsDirectory.jsx     # 17 records · category + cause chips · URL-state
│           ├── IndividualDetail.jsx           # type-aware: full for individuals, light for staff/advisor
│           ├── InstitutionDetail.jsx          # contract · partner · staff · participants · issues
│           ├── AdvisorPracticeDetail.jsx      # lead · co-advisors · clients · cohorts · partnered institutions
│           ├── OrganizationDetail.jsx         # Candid/GuideStar profile flow
│           ├── NotFoundCard.jsx               # shared invalid-id content card
│           └── sourceAccents.js               # shared source-accent map + resolveSourceAccent helper
├── data/
│   ├── unified/                    # the unified data layer (see §4)
│   │   ├── README.md
│   │   ├── types.js                # JSDoc typedefs for the 9 entities + ActivityItem
│   │   ├── sources.js              # SOURCE_SURFACE enum
│   │   ├── adapters/{enterprise,advisor,individual}.js   # source adapters
│   │   ├── synthetic.js            # synthetic seed bundle + Issue + ConnectionRequest seed
│   │   ├── assemble.js             # eager-evaluates all 4 sources, runChecks
│   │   └── index.js                # public read API (see §4)
│   ├── enterpriseFixtures.js       # athletes, workshops, exclusions, contacts,
│   │                               # endowmentSnapshot, current/priorCohortSnapshot,
│   │                               # dailyBriefItems, complianceAuditLog, reflections,
│   │                               # CURRENT_USER, T() / F() sector helpers
│   ├── clients.js                  # advisorPracticeProfile + 9 clients + stages + sectors + clientsByStage();
│   │                               # plus formatSessionDate(iso) — ISO→display formatter, timeZone:'UTC'
│   │                               # so the day stays stable regardless of local clock (ADV-012, bundle 12)
│   ├── cohorts.js, content.js, intakeData.js,
│   ├── lessonsData.js, orgsData.js, individualProfile.js,
│   ├── practiceContent.js, documentation.js, teamData.js,
│   └── cohortSignals.js, themes.js
└── contexts/                       # IntakeContext, CohortMemberContext, ModalStackContext, CommsContext
docs/                               # see §8
```

---

## 4. Unified data layer (`src/data/unified/`)

Adapter-over-existing pattern: customer-surface fixtures (`enterpriseFixtures.js`,
`clients.js`, `individualProfile.js`, `orgsData.js`, `cohorts.js`) are NOT
migrated. Each adapter reads its source and emits unified-shape records.
Assemble concatenates per-source bundles and wires deferred FKs.

### Nine entities

`persons`, `institutions`, `advisorPractices`, `programParticipations`,
`gifts`, `orgs`, `cohorts`, `connectionRequests`, `issues`.

Every record carries `sourceSurface` ∈ `'individual' | 'advisor' | 'enterprise'
| 'synthetic'`. IDs are namespaced: `p-{sourceSurface}-{native-id}`,
`gift-{sourceSurface}-{seq}`, `cr-synthetic-{seq}`, `issue-synthetic-{seq}`,
etc. **Same-person dedup across surfaces is deferred** — Marcus appears as
`p-individual-c-001`, `p-advisor-c-001`, AND `p-enterprise-1` (three Person
records, no merging).

**Shape note (Candid interleave 2026-06):** `Person.extensions.individual.causes`
is a string-ID array (e.g. `['education','sports','economic']`) — same shape as
`Org.causes`. Consumers resolve labels from the `CAUSES` taxonomy at render time,
so cause-label changes in `intakeData.js` propagate without re-shipping fixtures.

### Four source bundles (assembled into one store)

`enterprise` (21 persons + 1 institution + 16 participations + 18 gifts) ·
`advisor` (9 persons + 1 practice + 9 participations + 2 cohorts) ·
`individual` (1 person + 3 gifts + 17 orgs) ·
`synthetic` (51 individuals + 14 staff/advisor + 3 institutions + 6 practices
+ 51 participations + 105 ConnectionRequests + 14 Issues). Live composition:
**342 records across 9 entity types**.

### Five `runChecks` suites (all live, all passing)

- `assemble.runChecks` — composition integrity (sum-of-sources === assembled),
  global ID uniqueness within each entity type, FK orphan resolution across
  participations / practices / institutions / gifts / connection-requests /
  issues.
- `synthetic.runChecks` — entity counts, sourceSurface tags, FK resolution
  within bundle, first-name uniqueness + collision with real-person set,
  CR stage-count + monotonic timestamps + `gaveAt === Gift.date`, Issue
  enum + status/timestamp coherence + relatedEntity coupling.
- `{enterprise,advisor,individual}.runChecks` — per-adapter counts, FK
  resolution within bundle, self-tests on parsers (`parseGiftLabel`,
  `parseGiftDate`), informational `.info` (gift event-vs-field divergence,
  null-givingPlan count).

### Public read API (`unified.*`)

Direct entity arrays — `persons`, `institutions`, `advisorPractices`,
`programParticipations`, `gifts`, `orgs`, `cohorts`, `connectionRequests`,
`issues`.

Query helpers — `personsBy({type, sourceSurface})`,
`participationsByContext(contextId)`, `giftsByGiver(personId)`,
`countBy(entityName, predicate?)`, `byId(entityName, id)`.

Aggregate-default projections — `connectionFunnel()` (cumulative-reached
funnel, monotonically non-increasing), `connectionFunnelBy({sourceSurface})`,
`pilotMetrics()` (totalIndividuals, per-stage counts, conversions,
`totalDollarsAtGave`, `distinctOrgsAtGave`, `medianDaysMatchedToGave`),
`recentActivity({limit})` (sorted timestamp desc — CR stage transitions +
Issue events; gifts deduped via the CR `gave` transition),
`platformHealth()` (5 runChecks suites + composition + informational +
externalMonitoring), `openIssueCount()`, `openIssues()` (sorted openedAt
desc), `issueCountByStatus()`, `issueCountByCategory()`.

Record-level (explicit, not default landing data) —
`connectionsByGiver(personId)`, `connectionsByTarget(orgIdOrName)`.

---

## 5. Surface status

| Surface | Status |
|---|---|
| **Operations Overview** | **Complete and QA'd.** Overview ships 8 redesign slices (A–H, 2026-06); QA audit on branch `qa-audit-operations` (53 findings + 3 amendments = 56 total, doc at `docs/qa-audit-operations-2026-06-09.md`; **branch unmerged** by design). Fix bundles 1 (a11y highs QA-015–018), 2 (copy + structure QA-001/002/024/054 + lows 025–029), 3 (`801123a` — decision-free fixes QA-003/007/008/020/022/031/037–039/042/043/052, incl. deletion of the dead `UserList` stub), and 4 (`20354cc` — code quality QA-032/033/034/044/046/048/050/053, the `ExpandableRow` extraction, two QA-048 resolvers, three new tokens, and the Mission→Progression internal renames) banked. **Route-pages arc complete** (5 slices, 2026-06): slice 1 deleted the `health` tab + route stub, added Organizations to the nav, wired composition tiles as drills; slices 2–5 shipped live directory pages at `/operations/{individuals,institutions,advisors,organizations}` (77 / 4 / 7 / 17 records, all fixture-faithful with live-derived count headers). **Detail-routes arc complete** (6 slices + Candid interleave, 2026-06): per-record detail routes at `/operations/{dir}/:id` for all four entities — slice 1 (`2d1cf1c` — Institution detail, routing skeleton), 2 (`d327080` — AdvisorPractice detail), 3 (`abe5141` — Organization detail), 4 (`760af13` — Individual detail, type-aware: full view for `type='individual'`, light view for staff/advisor), Candid interleave (`3b7da48` — Organization detail restructured to the GuideStar/Candid profile flow: `foundedYear` literals added to `orgsData.js`, cause label "Economic" → "Economic Mobility" in `intakeData.js`, Person.causes normalized to a string-ID array in `adapters/individual.js`), 5 (`dacde68` — URL-filter state on all four directories: `q` debounced / `source` / `cat` / `causes` / `ids`-override, URL as source of truth, `replace` writes), and 6 (`743de6a` — drill wiring: rows clickable with filter persistence via `location.state.fromQuery`, shared `<AboutLine>` on Issue/Activity expand panels, pre-plan-clients drill behind a build-time derivation gate, ALL FOUR pilot tiles documented-unlinked by founder ruling with per-tile unlock conditions in code) banked. Chrome pattern across detail pages: `<BackLink>` with explicit `to` + `location.state.fromQuery` preservation, shared `<NotFoundCard>` for invalid IDs, dotted-bronze cross-links. **Fix bundle 5** (`e1e3600`) — founder visual cluster: QA-004 (Operations persona wired to `CURRENT_OPS_USER` in new `data/opsFixtures.js`); QA-014 reading A (activity-row chip drops border + adds `cursor:default`, surface accent moves from border to chip text color); QA-021 (muted text inside the Open issues tint card swapped to `--sh-text-secondary` — both the IssueRow meta line and the "Per-issue detail view coming soon." footnote — contrast moves from ~3.46:1 to ~8:1 against `--sh-bg-tint`); QA-030 ruling b1 (`SURFACE_COLORS.Operations` swapped from `--sh-bronze` to `--sh-text-secondary` — Operations now reads quiet internal, distinct from Advisor's bronze). Wontfix per founder ruling: QA-011 (no new funnel footnote, consistent with the pilot-tile decision), QA-013 (SuiteRow copy-to-clipboard defer until attention-state fires), QA-040 (asymmetric `<main>` padding by design), QA-045 (date-helper overlap acceptable per audit), QA-051 (no React.memo at current scale), QA-035 (bronze progression bars on tint track — editorial restraint, 3:1 non-text contrast met), QA-036 (expand-panel prominence asymmetry — intentional: Open issues is the higher-priority attention signal). **Audit posture: 55 of 56 findings resolved**; remaining 1 — QA-023 blocked on a future CR-level filtered view. |
| **Enterprise** | **Built and audited.** All 6 sections live (Overview, Roster, Program, Compliance, Reports (+ 5 sub-pages), Setup wizard). QA audit on branch `qa-audit-enterprise` (**157 findings** — 9 Critical + 33 High + 60 Medium + 55 Low — doc at `docs/qa-audit-enterprise-2026-05-30.md`; **branch unmerged** by design). **Count guard:** the audit's executive-summary "Medium: 76" is a typo (actual Medium = 60, verified by counting numbered lines); CLAUDE.md previously inherited the typo as "173 findings" — corrected here. The 26 Compliments listed in the audit are observations of working-well things, not findings, and are counted separately. **Enterprise does NOT use Advisor's numbered-bundle model.** Criticals + Highs shipped as direct per-finding-or-grouping commits; Mediums + Lows went through the cluster-based triage in `docs/qa-triage-medium-low-2026-05-30.md`. **9 Criticals shipped** (`33cb42f` covered all 9; `3bb9cb1` later completed Critical #9's full activity-log reconciliation as a side effect of fixing Medium #91). **32 of 33 Highs shipped — 1 deferred (#12)** post Sweep 1 + Sweep 2. The original 26 (pre-Sweep) were addressed via `33cb42f` (Criticals bundle's secondary effects), `fd7b2b0` (Diane Okonkwo normalization, #17 + #29), `a525b29` (compliance officer + Andre/Ava + Mia/Jordan + CohortComparison yoy, #18/#21/#23/#24), `be6febc` (ISO dates + cohort framing, #19/#20), `aa56b64` (engagementTimeline derivation, #22), `d0e82fb` (React keys + responsive padding, #25-#28 + #32), `542e893` (ModalStack perf, #42), `bc0beb9` (persona canonical, #31 as side effect), and the `7998c7d` "narrative re-authoring" sweep that closed the voice/internal-leak cluster #30 + #33-#41 (10 items). **Sweep 1** (`daf8ac4`) — token-discipline sweep: closed the 5 brand-token Highs — **#10** (HelpIcon border `#B8AE9E` → `--sh-bronze-border`, accepted visual shift to warmer/lighter accent), **#11** (Tag.jsx accent palette → 3 existing tokens `--sh-divider` / `--sh-text-secondary` / `--sh-bronze-border`), **#13** (Button.jsx primary text `'#FFFFFF'` → `--sh-text-on-accent`), **#14** (SetupWizard stepper text `'#FFFFFF'` → `--sh-text-on-accent`); **deferred #12** (Tag `warning` palette — consumed by paused Individual `Team.jsx`: keep palette, add `--sh-warning-bg/text/border` tokens when Individual resumes — don't remove live-but-dormant code). Sweep 1 ALSO dispositioned **all 11 BATCH Cluster IV items**: substantive token swaps for #58 (Modal padding) / #59 (DailyBrief marginTop) / #60 (EnterpriseProgram minWidth) / #61 (SetupWizard space) / #62 (SetupWizard radius) / #66 (HelpIcon 9px → `--sh-text-xs` F1-nudge per advisor ADV-006 precedent) / #68 (new token `--sh-border-accent-deep: 4px solid var(--sh-bronze-deep)`, matching the bundle-4 `--sh-border-accent` shape; single consumer at PhilanthropicReadiness); partial: #63 (6 of 8 sites swapped to `--sh-radius-md`, 2 Individual sites deferred per paused-surface ruling) + #67 (2px → `--sh-space-half` clean swap; 72px + 6px documented as contained primitives with inline comments); documented-as-contained-primitive: #64 (Button padding triple, per audit's own ruling) + #65 (Chrome 34px avatar, inline comment). **Sweep 2** (`03b77a9`) — a11y Highs: **#15** EnterpriseRoster `<tr>` keyboard support (`tabIndex={0}` + `onKeyDown` Enter/Space with `preventDefault` + `aria-label`; row semantics preserved — no `role="button"` override per WAI-ARIA tabular-data practice, distinct from OperationsSurface `ExpandableRow`'s div-as-button pattern); **#16** HelpIcon a11y additions (`aria-expanded` + `aria-controls` via `useId` + Escape-close `onKeyDown`; `onBlur` preserved). **HelpIcon cross-surface note:** currently consumed only by Advisor's `Pipeline.jsx` (2 sites — `:86` + `:445`); Sweep 1 + Sweep 2 changes benefit that surface + any future Enterprise consumer that picks up the shared component. **Sweep 3** (`ed58670`) — Cluster II dead-code cleanup: 6 removals — **#49** (unused `statusFor` import in EnterpriseOverview), **#50** (uncalled `SelectField` component, SetupWizard), **#51** (uncalled `RadioGroup`/`Radio` components + 4 confined styles, SetupWizard), **#52** (unimported `CardHeader` function, Card.jsx — 33 lines; the `Card` primitive itself + the `accent` prop kept), **#53** (unconsumed `depth` field in ModalStackContext — removed from default context + value object + deps array + JSDoc typedef), **#54** (`inProg` un-exported in enterpriseStats.js — visibility tightening; const stays, just no longer surfaced to consumers). **#55 reclassified NOT-A-FINDING**: the audit flagged `Card`'s `accent` prop as dead, but a live consumer at `OperationsSurface.jsx:275` (Open Issues bronze accent stripe) post-dates the 5/30 audit — the prop is in active use. #55 joins the **#31 / #93 / #94 "audit pre-dated the consumer/fix" pattern** — the audit captured a concern that subsequent independent work resolved, so the disposition is closed-as-not-finding rather than closed-as-fixed. **Net −136 lines** for the Sweep 3 commit. **Sweep 4** (`d90c513`) — Cluster V (React keys / patterns, all 5) + Cluster III (perf tweaks, both 2): **#69** (curriculum `key={i}` → `key={title}` across EnterpriseProgram + SetupWizard, 2 sites), **#70** (BarChart `key={i}` → `key={labels?.[i] ?? i}` with defensive fallback for the undefined-labels case; 2 sites — BarSlot + xAxis span), **#71** (WorkshopCalendar 42-cell grid `key={i}` → `key={key}` reusing the already-computed `dateKey(...)` local var; the highest-value correctness fix — month-nav was relying on positional index across re-renders), **#72** (Modal `titleIdRef` switched from `useRef(\`modal-title-${Math.random()...}\`)` inline template to the null-guard pattern matching the sibling `idRef`; same rendered id stability, no per-render template re-eval), **#73** (Chrome `UserIdentity` refactored — extracted `ClickableUserIdentity` subcomponent so the `useState` hover allocation happens only on the clickable branch; DOM render-equivalent for both branches, verified by visual smoke + grep on `gap: var(--sh-space-3)`), **#56** (`engagementMin` + `engagementMax` hoisted to module-level exports in `shared/enterpriseStats.js`, consumed by EnterpriseOverview + ProgramSummary BarChart ariaLabels — was `Math.min(...spread)`/`Math.max(...spread)` per render; static fixture data, computes once at module load now). **#57 SKIPPED-PREMATURE** per the QA-051 "no React.memo at current scale" lens (N=5 workshops, 42 grid cells; documented, no code). **No new tokens this sweep.** Notable artifacts: `ClickableUserIdentity` in Chrome.jsx, `engagementMin` / `engagementMax` consts in enterpriseStats.js. **Cluster VI** (`ed29a80`) — voice/tone cleanup of the largest editorial bloc (18 items): **12 platform-voice fixes** + **6 not-a-finding closes across two distinct sibling patterns**. **12 fixed**: **#74 + #75** (4 report h1s + REPORT_CARDS Title Case → sentence case — "Program summary" / "Cohort comparison" / "Philanthropic readiness" / "Program outputs"); **#78** (`WorkshopDetail.jsx` `Follow-ups · {n}` middle-dot → `Follow-ups ({n})` parens — middle dot is overloaded elsewhere as a metadata-item separator); **#83** (`EnterpriseCompliance.jsx:106` "tamper-resistant audit log" duplication trimmed — the phrase already lands at `:93`, so `:106` was reduced to "This audit log is read-only in production"); **#85** (`EnterpriseCompliance.jsx:60` product-noun leak "Give Screen" → user-facing "when choosing a gift target" — same class as Highs-tier #41 "Individual surface" leak); **#87** (daily-brief coined "uncontacted N days" → "— no contact in N days"); **#88** (`EnterpriseProgram.jsx:39` subtitle dropped "participating" — `{N} athletes · {term} · {dateRange}` now reads as three parallel noun phrases); **#101** (`AthleteProfile.jsx:93` single-athlete caption grammar fix — "Athlete's" singular possessive paired with "their" plural pronoun was contextually wrong on a single-athlete profile; switched to "Their own words on their philanthropic practice..."); **#102** (`Endowment.jsx:73` "interactive modeling" sell-word → "projection model"); **#104** (`EnterpriseProgram.jsx:74` "16-lesson v1 curriculum" version leak → "16-lesson curriculum"); **#77** (`CohortComparison.jsx:142` "not measuring achievement" → "not ranking athletes" — **explicitly NOT the audit's proposed "merit"**, which reads MORE evaluative than "achievement" and would have moved the wrong direction); **#79** (`enterpriseFixtures.js:445` Workshop 2 note `"2 absent (scheduling conflict). Recording shared."` → `"Two absent — scheduling conflict. Recording shared."`). **6 not-a-finding closes across TWO distinct sibling patterns** — both record reasons a finding closes as not-a-finding, but for different underlying causes: **(a) AUTHORED-VOICE EXEMPT (3)**: **#80** (operator-authored `action` fields on workshop follow-ups — imperative-fragment register is the correct register for action items written by a staff `owner`), **#81** (operator-authored note labels at `enterpriseFixtures.js:280` activity-log entry + `:459` workshop attendance reason — both are operator-written records displayed back), **#100** (`enterpriseFixtures.js:733` Marcus first-person reflection rendered in `AthleteProfile.jsx:88-104` under SectionLabel "Reflections" with the in-code marker `{/* Reflections — athlete's own first-person voice (Path B) */}` — the strongest case in the cluster, since Path B is explicitly invoked in code to mark this content as authored-voice exempt). This trio parallels Advisor **ADV-014** (PracticeHome journal voice) + **ADV-021** (Marcus private-notes voice) — Path B governs **platform-authored** copy, NOT in-character seed content displayed back to its author. **(b) INTENTIONAL-PRECISION (3)**: **#84** (`PhilanthropicReadiness.jsx:74` "Counts are mutually exclusive across stages" — methodology footnote; "mutually exclusive" is the precise statistical term, softer wording would trade precision for warmth in a stat-method context where precision is doing real work), **#86** (`Endowment.jsx:64` 5% payout-rule paragraph — intentional legal/financial precision; the section already carries the `:189` "Not investment advice... subject to legal review prior to partnership finalization" disclaimer, so the compressed technical voice is structurally load-bearing), **#89** (`enterpriseFixtures.js:546` lowercase "Cooper State athletics" — descriptive common-noun reading ("the athletic program at Cooper State"); the capped instances are the proper-noun org name ("Cooper State Athletics Booster Association"); the audit's call for uniform Title Case would erase a grammatically defensible distinction). **Pattern note**: the not-a-finding bookshelf now carries TWO sibling patterns — **audit-pre-dated-the-consumer/fix** (#31 / #55 / #93 / #94 — concern was real at audit time but subsequent independent work resolved it) and **authored-voice-exempt** (#80 / #81 / #100 — Path B's platform-voice scope doesn't reach in-character authored seed content displayed back to its author, ADV-014/021 sibling). Two different reasons a finding closes as not-a-finding. **Minor-cluster sweep VII + VIII + IX** (`8ecdaf3`) — 12 items across the three minor BATCH clusters: **9 fixed** + **3 not-a-finding closes**. **9 fixed**: **#95** (`SetupWizard.jsx:283` empty `onClick={() => {}}` dropped — Button doesn't require a handler; the `disabled` prop carries the intent); **#97** (`EnterpriseCompliance.jsx:20` session id ``Date.now()`` → `crypto.randomUUID()` — eliminates the double-click-in-same-ms collision possibility); **#108** (Modal `closeFocused` state + inline outline removed — the global `:focus-visible` rule at `global.css:59-60` now governs the close button; visible UX delta: ring shows on keyboard Tab navigation but NOT on mouse-open modal mount, aligning with `:focus-visible` spec intent; 8 modal consumers benefit); **#111** (`ComposeMessage.jsx` fixed `compose-recipients-options` datalist ID → `useId()` — resolves modal-stacking collision risk); **#112** (`HelpIcon.jsx:52` `cursor: 'help'` → `'pointer'` — the icon is a click-toggle popup, not a hover-help affordance); **#114** (`EnterpriseCompliance.jsx:89-108` audit-trail Card outdented 2 spaces to 6-space sibling indent matching its actual JSX depth — pure cosmetic, zero behavior); **#116** (`Card.jsx:4` 2-line comment documenting that `hovered` state is always allocated but only consumed when `isClickable`; **InteractiveCard extraction flagged as a Cluster I candidate** — same #73 ClickableUserIdentity precedent, but bigger than minor-cluster scope); **#123** (`enterpriseFixtures.js:641` `programTerm` middle-dot → em-dash `'Season Residency — Aug 2025 to May 2026'` to match the `INST_PROFILES.contract` convention that the `EnterpriseProgram.jsx:28` parser depends on via `.split(' — ')`); **#96** (`enterpriseFixtures.js` `exclusions[]` **EXPANDED 3 → 6** — added Quick Cash Sports Loans LLC (id 4, EIN `04-3367892`), TigerBet Online Sportsbook (id 5, EIN `04-2294516`), Premier Athletic Apparel Co (id 6, EIN `04-1145037`) as real exclusion records so audit-log entries `audit-002`/`003`/`004` now reference orgs that EXIST in the live list — **this is a visible compliance-integrity fix, not hygiene**: the audit trail UI was previously claiming exclusions that weren't in the list. All 3 new connectionDetails use the neutral disclosure-model framing ("Athletes still see the org in their giving view... — disclosure model, not blocking") consistent with the original 3 entries and the #85/Path B ethos). **3 not-a-finding closes**: **#98** (`EnterpriseProgram.jsx:28` + `EnterpriseSurface.jsx:29` `contract.split(' — ')` parses — both sites already carry defensive fallbacks; **joins the audit-pre-dated-the-consumer/fix pattern** alongside #31/#55/#93/#94 as the 5th member); **#127** (`AthleteProfile.jsx:24` + `UserProfile.jsx:20` `useComms()` called unconditionally before any early-return — this is **mandatory** per React Rules of Hooks; the audit's recommendation would itself violate React. Any outside-Provider defense belongs inside `useComms()`, not the consumer call site. **NEW close-reason pattern: AUDIT-FRAMING-WRONG**); **#90** (mixed integer baseline IDs vs string IDs across fixtures — the audit asked to standardize NEW entities to strings, NOT to migrate the integer baseline; every new entity introduced since the audit (unified-layer namespaced IDs, clients, cohorts) already uses string IDs, so the forward policy is already in place. Closes as forward-policy-already-in-place, sibling of AUDIT-FRAMING-WRONG since the audit's read of the policy was off). **NOT-A-FINDING TAXONOMY** — the Enterprise arc surfaced FOUR distinct close-reason patterns. All record "the audit flagged X but X is not a defect," but for different underlying causes; future sessions and re-audits should not treat any of these as open defects without re-checking against the relevant pattern: **(1) AUDIT-PRE-DATED-THE-CONSUMER/FIX** (5 items: #31, #55, #93, #94, #98) — the audit captured a real concern at audit time, but subsequent independent code resolved it; **(2) AUTHORED-VOICE-EXEMPT** (3 items: #80, #81, #100) — Path B governs **platform-authored** copy, not in-character seed content displayed back to its author (workshop staff action-item fragments, operator-written note labels, athlete first-person reflections); direct sibling of Advisor's ADV-014 (PracticeHome journal voice) + ADV-021 (Marcus private-notes voice); **(3) INTENTIONAL-PRECISION** (3 items: #84, #86, #89) — methodology / legal / grammatical-distinction language that should stay precise; softening would trade meaning for warmth in contexts where the precision is doing real work (stat-method footnotes, legal/financial payout-rule disclosures, grammatically defensible proper-noun vs common-noun distinctions); **(4) AUDIT-FRAMING-WRONG** (2 items: #127, #90) — the audit's recommended fix is itself incorrect: would violate a platform invariant (React Rules of Hooks) or misread the existing policy (the standardize-NEW-entities ask was already in force). Closes as the recommendation-itself-is-broken pattern. **Cluster I** (banks `42354ba` → `36a13fa` → `65eb7fd` → `3afe4a0`) — shared-primitive extractions, **the last substantial Enterprise slice** executed as **4 isolated sub-slice banks**. **4 extracted + 1 DEFERRED + 1 NOT-A-FINDING (paused-surface)**: **#45 MessageHistoryCard** (`42354ba`) — extracted verbatim Card block + 6 style consts from `AthleteProfile.jsx` + `UserProfile.jsx` into shared `src/components/MessageHistoryCard.jsx`; pure presentational, takes `messages` as a prop; `useComms()`/`getThread()` STAYS in each parent (Rules-of-Hooks discipline, #127 sibling — pulling the hook into the new component would force every consumer to be inside a CommsProvider); net −120 lines. **#43 DataTable** (`36a13fa`) — extracted shared `src/components/DataTable.jsx` from EnterpriseRoster + ProgramOutputs + CohortComparison; columns API with `lead`-serif + `nowrap` per-column flags (Organization wraps, Sport nowraps); `onRowClick` preserves EnterpriseRoster's Sweep-2 #15 row keyboard-activation a11y (`tabIndex={0}` + Enter/Space + `preventDefault` + `aria-label`, row semantics preserved with NO `role="button"` override per WAI-ARIA tabular-data practice); `minWidth` param (880px Roster, 560px reports default); net −189 lines. **#44 Tag tracking extension** (`65eb7fd`) — extended `Tag.jsx` with `tracking='loose'` preset (letterSpacing `0.06em` — midpoint of pre-existing 0.04em role pills and 0.08em status pills; padding `2px 8px`; weight 500; no border) PLUS additive `style` prop (for ContactsDirectory `flexShrink:0` and EnterpriseProgram `marginLeft`); migrated 6 inline bronze-tint pills (ContactsDirectory `rolePillStyle`, UserProfile `rolePillStyle`, EnterpriseCompliance `sessionPillStyle` SESSION, EnterpriseProgram `pendingPillStyle` "Pending review", Endowment `reviewPillStyle` REVIEW PENDING, SetupWizard `pendingPillStyle` PENDING); Tag's 4 EXISTING Advisor consumers (CurriculumLibrary ×3, DraftsList ×1) **UNAFFECTED** — default tracking unchanged, render byte-identical; net −52 lines. **#48 SegmentedControl** (`3afe4a0`) — extracted **ENTERPRISE-SCOPED** `src/components/SegmentedControl.jsx` from SetupWizard + Endowment (the bordered-jointed treatment) using **Endowment's N-button `borderRight` convention** (generalizes to N > 2; SetupWizard's logic produced double-seams in N > 2) plus a **FREE A11Y UPGRADE** (`role="group"` + `aria-labelledby` + `aria-pressed` per button — the Enterprise pair previously had NONE); `size="sm"` covers SetupWizard padding 4px 12px, default `size="md"` covers Endowment 6px 14px; consumers thread `ariaLabelledBy` via `useId()` against the visible label span; also renamed Endowment's control label `"Term horizon"` → `"Commitment term"` (advisory-team converged copy ruling per Parker/Morgan: relational partnership register, names what the department is committing to — not investment-product voice like "endowment term" or "contribution length"); net −59 lines. **#48 CROSS-SURFACE DIVERGENCE FINDING (important for future re-audits)**: the 5/30 audit framed #48 as a SetupWizard/Endowment `borderRight`-convention disagreement, but pre-build investigation found the 4 SegmentedControl consumers carry **THREE distinct visual treatments** — pill (Advisor `LessonEditor.jsx:365`), fused-rectangle (Advisor `Pipeline.jsx:456`), bordered-jointed (Enterprise pair). The Advisor treatments are **deliberately distinct for different UX contexts** (LessonEditor's category/scope picker vs Pipeline's drawer state-picker), NOT drift to converge. **Ruling: Option C — scoped #48 to the Enterprise pair only; Advisor's `LessonEditor` + `Pipeline` local `SegmentedControl` function components left BYTE-UNCHANGED** (banked Advisor surface preserved, zero regression risk). All 4 consumers share the API + a11y substrate (`options`/`value`/`onChange`/`ariaLabelledBy`, `aria-pressed`, `role="group"`) but NOT the visual treatment. **ADV-044** (radiogroup-vs-aria-pressed deferred decision) stays a single Advisor concern, **NOT resolved** by this extraction. **Future sessions: do NOT re-flag Advisor's two SegmentedControls as un-DRY'd — the divergence is intentional.** **Two non-extracted Cluster I dispositions**: **#47 StatusDot/Bullet — DEFERRED** (2 consumers — `WorkshopDetail.jsx:203-212` status-variant 3-state dot vs `DailyBrief.jsx:165-173` decorative bullet — are semantically different, not real DRY; the #57 / QA-051 premature-abstraction lens applies. Revisit only if a 3rd genuine status-dot consumer appears with the same shape). **#116 InteractiveCard — NOT-A-FINDING (paused-surface)**: multiline grep confirms all 3 `interactive`/`onClick`-bearing Card consumers (`IndividualSurface.jsx:257`, `IndividualSurface.jsx:446`, `Learn.jsx:86`) are in frozen Individual v0.6.1; **ZERO active consumers** in Enterprise / Advisor / Operations / shared components. **4th paused-surface ruling**, sibling pattern alongside #12 (Tag warning palette) + #63 (2 Individual radius sites) + Tag-warning (same #12). `Card.jsx:4` comment (added during the minor-cluster sweep) already documents the consumer set; no extraction performed. Closes when Individual unpauses. **Cluster I net impact**: 4 new shared primitives in `src/components/` (`MessageHistoryCard`, `DataTable`, `Tag` tracking variant, Enterprise-scoped `SegmentedControl`), **~420 lines of duplication collapsed**, +1 free a11y upgrade (Enterprise SegmentedControl pair gained `aria-pressed` + `role="group"` + `aria-labelledby` they previously lacked). **115 Mediums + Lows triaged** into 5 disposition buckets: **ALREADY-CLOSED (6)** — implemented incidentally before triage (includes #93 + #94 moved here from FIX-NOW after Cluster A shipped, per the triage doc's own Update note). **FIX-NOW (3 findings remaining post-Cluster-A-ship)** — all SHIPPED: Cluster B (#91 lesson-count reconciliation) via `3bb9cb1`; Cluster C (#76 Reports card "performance" + #82 W2 "stalled" voice) via `a1b505b`; Cluster A (#93 Chrome persona + #94 SetupWizard Diane) already shipped via `bc0beb9` and moved to ALREADY-CLOSED. **WON'T-FIX (41)** — audit-marked accept / by-design / verification-only observations, resolved by disposition (no code needed). **DEFER (5 M+L)** — revisit on specific later conditions (mobile QA pass, advisor Documentation rework, bundle-size threshold, etc.). **BATCH (60) — 60 dispositioned across Sweeps 1 + 3 + 4 + Cluster VI + minor-cluster sweep VII+VIII+IX + Cluster I, 0 REMAINING**: Sweep 1 resolved Cluster IV (11: token discipline); Sweep 3 resolved Cluster II (7: dead-code cleanup — 6 removed + #55 reclassified not-a-finding); Sweep 4 resolved Cluster V (5: React keys / patterns, all fixed) + Cluster III (2: perf tweaks — 1 fixed, 1 documented-skip); Cluster VI resolved (18: voice / tone — 12 fixed + 6 not-a-finding across the two sibling patterns above); minor-cluster sweep resolved Cluster VII (4: code-quality — 2 fixed + 2 not-a-finding), Cluster VIII (3: data-consistency — 2 fixed + 1 not-a-finding), Cluster IX (5: misc Low — all 5 fixed); Cluster I resolved (5: shared primitive extractions — 4 extracted + #47 DEFERRED + #116 NOT-A-FINDING paused-surface). **All 9 BATCH clusters (I–IX) closed.** **Naming-scheme note:** FIX-NOW uses LETTER clusters A/B/C (all shipped); BATCH uses ROMAN-NUMERAL clusters I–IX. Enterprise still does NOT use Advisor's numbered-bundle model — "**Sweep N**" is a distinct sequencing label introduced for the Enterprise-depth work, parallel to but distinct from Advisor's "Bundle N" cadence. **Audit posture: 156 of 157 SHIPPED-or-dispositioned-or-deferred-or-not-a-finding — 9 Critical + 32 High shipped + 1 High deferred (#12) + 6 ALREADY-CLOSED + 3 FIX-NOW + 41 WON'T-FIX + 5 DEFER (M+L) + 60 BATCH dispositioned (Cluster IV=11, II=7, V=5, III=2, VI=18, VII=4, VIII=3, IX=5, I=5). #12 is the only open implementation item (deferred High, paused-Individual-gated — sibling of #116 / #63 / Tag-warning paused-surface pattern; closes when Individual unpauses).** Arithmetic: 9+32+1+6+3+41+5+60 = 157 all-dispositioned ✓; BATCH internal sum 11+7+5+2+18+4+3+5+5 = 60 — every BATCH cluster closed. **Enterprise depth arc COMPLETE.** Cluster I net impact (restated): 4 new shared primitives (`MessageHistoryCard`, `DataTable`, `Tag` tracking variant, Enterprise-scoped `SegmentedControl`), ~420 lines duplication collapsed, +1 free a11y upgrade. Fixture dates are uniformly shifted −192d for coherence with today (`Enterprise fixture date coherence` commit `7998c7d`). |
| **Advisor** | **Built and audited.** 8-section IA (PracticeHome, ClientRoster, ClientWorkspace, CurriculumLibrary, CohortSpace, Pipeline, Documentation, PracticeSettings; plus auxiliary DocCreate/DocDetail/LessonDetail/LessonEditor/DraftsList/CohortDetail). All 9 clients seeded with distinct fictional substance per the locked roster. QA audit on branch `qa-audit-advisor` (41 findings, doc at `docs/qa-audit-advisor-2026-06-13.md`; **branch unmerged** by design — the audit doc's executive-summary "43 unique IDs" is a typo, verified during tail-triage: ADV-034 and ADV-037 never existed at any heading level or as cross-references; actual H3-header count is 41). Fix bundles 1 (`623c1ab` — copy sweep: ADV-001 "Track and Field" title-case, ADV-003 "Section N" kicker leak removed from 10 user-facing sites), 2 (`c941df3` — brand-token sweep + `StateBadge.jsx` extraction (ADV-004/005/008); three new tokens added: `--sh-state-active-bg` and `--sh-state-active-text` (the system's first non-warm semantic colors) plus `--sh-text-on-accent`), 3 (`5c6f3e7` — pixel-literal sweep (ADV-006); ADV-026/028 deferred to the ADV-025 drawer slice; ADV-033 880px wrapper closed as intentional editorial choice), 4 (`776fa4d` — glyph→SVG sweep (ADV-007 in-scope, 6 sites); established the platform's first shared icon registry at `src/components/Icon.jsx` (`chevron-right`, `chevron-left`, `plus`; idiom seeded by `BackLink.jsx`); `?` re-classified not-finding (standard ASCII punctuation, not icon-substitute); PracticeHome:148 / Pipeline:321 / DocCreate:179 carried to bundles 7 / 6 / 5), 5 (`27e1c8c` — a11y cluster: ADV-016 (search aria-label), ADV-031 (section-error `role="alert"`), ADV-035 (flag-toggle aria-pressed), ADV-039 (LessonDetail file/title aria-labelledby via `useId`), ADV-036 (DocCreate + LessonEditor FormField label association via guarded `cloneElement` onto native input/textarea/select children only — wrapped non-native children pass through unchanged); opened **ADV-044** (SegmentedControl pick-one / aria-pressed semantic mismatch — deferred) and applied SC-α group-labels to LessonEditor's SegmentedControl as the first coverage), and 6 (`2e30b24` — Pipeline ConfigDrawer a11y: ADV-025 focus trap via a new shared hook `src/components/useDialogA11y.jsx` (mirrors `Modal.jsx`'s trap but with a `.contains()`-aware boundary check that's robust to SVG-only button content — a strict improvement over Modal's text-node-dependent comparison, ready for a future Modal-DRY slice; Modal.jsx itself untouched, so the 8 existing Modal consumers carry no regression risk); ADV-026 (two new tokens `--sh-overlay-drawer` + `--sh-shadow-drawer` — distinct from `--sh-overlay-bg`/`--sh-shadow-modal` for the side-drawer geometry); ADV-028 (transition → `--sh-transition-fast`, F1-precedent nudge); the `×` close glyph → Icon registry (added `close`); SC-α group-labels on Pipeline's SegmentedControl AND RadioList), 7 (`226af00` — ADV-009 fake-control removal: 5 dead controls deleted — PracticeHome "New entry", PracticeSettings Change/Change/Configure on the Working preferences card, CohortSpace "Start a cohort" — plus the forced layout cleanup each removal left: vestigial single-child flex wrapper collapsed in CohortSpace, now-purposeless `marginBottom` dropped on PracticeHome's journal meta line, orphaned `Button` import removed from CohortSpace; the advisor surface is now affordance-honest — every visible button has a real handler), 8 (`0a39530` — ADV-015 URL-state sync on ClientRoster: `q` (debounced, 250ms) / `stage` / `sport` synced to the URL via `useSearchParams`, ported faithfully from the Operations slice-5 pattern (URL as source of truth, `replace` writes, default-omission for clean URLs, inline `parseSingleSelect` for the single-select-with-"All"-sentinel shape vs Operations' Set-based multi-select); PracticeHome's 4 stage Stat-card links updated to **C-α literal casing** (`?stage=Active` not `?stage=active`) so they round-trip through the validation; ClientRoster's filtered views are now shareable and refresh-survivable; the Operations slice-5 URL-state pattern now has **5 in-tree consumers** (4 Operations directories + ClientRoster), giving a future `useDirectoryFilters` hook its consumer set for a DRY-extraction slice when prioritized), 9 (`a3654c1` — tail-triage copy/structure: ADV-002 (CohortSpace decorative `<SectionLabel>` → plain `<p>` eyebrow, fixing an h2-above-h3 semantic inversion; the 5 legitimate CohortDetail `<SectionLabel>` section headings left untouched) + ADV-022 (CurriculumLibrary stat-row parallelism "Base/Tailored/Authored" + body-copy "forks"→"tailored lessons" terminology alignment)), 10 (`7c4bf78` — tail-triage affordance honesty: ADV-013 (dropped the false "4 entries this month" journal count from PracticeHome) + ADV-024 (removed the two always-empty CohortDetail Curriculum-track + Sessions sections — no data path, no write affordance; same spirit as ADV-009 bundle 7)), 11 (`41786b9` — tail-triage brand consistency: ADV-020 (`window.confirm()` → Modal-based discard flow using `Modal.jsx` directly, untouched; the advisor surface now has **zero native browser confirms**); ADV-030 closed as not-finding — the inline "+ New section" button is a deliberate visual pairing with the adjacent "+ New document" `<Link>` sibling, converting to `<Button>` would break the pairing and lose the bronze), and 12 (`2c89164` — tail-triage data + a11y: ADV-011 (live `new Date()` → static seed-aligned `'Monday, May 4'` demo date; verified Monday via UTC parse) + ADV-012 (9 client `nextSession` fixtures migrated to ISO `'YYYY-MM-DD'`, new `formatSessionDate(iso)` helper exported from `src/data/clients.js` with `timeZone:'UTC'` for day stability, 4 display sites wrapped — spec-guaranteed parse, byte-identical display output) + ADV-041 (field-level minutes error inside the FormField with `role="alert"` + `aria-invalid` + `aria-describedby` mirroring bundle 5's Documentation section-error a11y pattern; `canSave`/disabled logic unchanged — purely additive feedback)) banked. Locked dispositions: ADV-009 (fake-interactive controls) → **5 removed in bundle 7** (PracticeHome / PracticeSettings × 3 / CohortSpace) **+ 4 Stage Rename buttons carved out as a future sibling slice** per CLAUDE.md §7 ("renameable per advisor preference") — the 4 render from a single `stages.map()` at `PracticeSettings.jsx:57` and are deliberately preserved; **6 closed as not-finding or intentional** — ADV-014 (PracticeHome journal voice) + ADV-021 (Marcus private-notes voice) closed because Path B governs platform-authored copy, not advisor-authored seed displayed back to the advisor (advisor-internal voice); ADV-033 (`maxWidth: '880px'` on PracticeSettings) closed as intentional editorial choice; **ADV-010** (5 local-only state sites with honest disclosure copy) closed per audit author's own "acceptable per prototype scope" framing; **ADV-038** (LessonDetail body placeholder) closed for the same disclosure-acceptable framing; **ADV-030** (Documentation inline "+ New section" button) closed because it's a **deliberate visual pairing with the adjacent "+ New document" `<Link>` sibling** — converting to `<Button>` would break the pairing and lose the bronze; the audit's "inconsistent with `<Button>`" framing missed the local Link↔button consistency the design achieves by intent. **2 deferred** — ADV-019 (3 code-comment "Section N" leaks in ClientWorkspace) folded into a future code-quality refactor slice (not user-facing, no urgency); **ADV-044** (SegmentedControl + RadioList single-select with `aria-pressed` instead of radiogroup semantics) open and deferred — applies to all three single-select controls (LessonEditor SegmentedControl, Pipeline SegmentedControl, Pipeline RadioList); full radiogroup conversion is a future micro-slice. **Audit posture: 33 of 42 dispositioned — 25 fixed (ADV-001/002/003/004/005/006/007/008/009/011/012/013/015/016/020/022/024/025/026/028/031/035/036/039/041), 6 closed as not-finding or intentional (ADV-010, ADV-014, ADV-021, ADV-030, ADV-033, ADV-038), 2 deferred (ADV-019 to a future code-quality slice; ADV-044 to the radiogroup decision).** (**Denominator is 42** — 41 audited findings (the audit summary's "43" is a typo; ADV-034 and ADV-037 never existed) plus ADV-044 opened during bundle 5.) **The Advisor QA arc — audit + all 12 fix bundles + tail triage — is COMPLETE. No planned QA fix bundles remain.** Remaining items are **feature / decision / future-triage, not QA fixes**: **Stage Rename sibling slice** (feature — wire the 4 carved-out `PracticeSettings.jsx:57` Rename controls to a real Modal-based rename flow with state + persistence per CLAUDE.md §7); **ADV-044 radiogroup micro-slice** (decision — accept the `aria-pressed` debt as documented, OR convert all three single-select controls to `role="radiogroup"` + `role="radio"` + `aria-checked` + arrow-key handling). Two **new known-issues discovered during the tail triage** (not part of the audit's 42) — **both now CLOSED**, thread complete: **(a)** `cohort.nextSession` at CohortDetail + CohortSpace — **CLOSED** (`2a6230c`): migrated both `cohorts.js` fixtures (coh-001 `'May 18, 2026'` → `'2026-05-18'`, coh-002 `'May 27, 2026'` → `'2026-05-27'`) to ISO and wrapped the 2 display sites (`CohortDetail.jsx`, `CohortSpace.jsx`) with `formatSessionDate` — the exact ADV-012 substrate. Adapter passthrough (`adapters/advisor.js`) now carries ISO with an inline note; zero unified consumers. Render byte-identical. `formatSessionDate` now has **3 advisor cross-importer files** (future shared-util-extraction trigger candidate per #47/#57, NOT done now). **(b)** LessonEditor **title/minutes field-error inconsistency** — **CLOSED** (`0df9be3`): gave the Title field the same field-level inline error treatment as the Minutes field (ADV-041 pattern) — `titleErrorId` via `useId`, `aria-invalid` + conditional `aria-describedby` on the input, and a field-level `<p role="alert">Title is required.</p>` with byte-identical muted-italic typography. Reuses the existing `titleValid` check; `canSave` / `invalidReason` / global-hint / save logic untouched (purely additive a11y feedback). Isolated to `LessonEditor.jsx`. Shared error-helper extraction **NOT done** — 2 consumers (Minutes + Title) is below the #47/#57 threshold; a future 3rd field-level error is the trigger (`FieldError` or `FormField` error-prop), document-don't-build. **CONTINUING 2026-07-01 SESSION — Advisor persistence arc OPENED (investigation-only, no build yet).** Full write-up lives in the Individual row's 2026-07-01 continuation (item 10). Cross-reference facts here: `AdvisorSurface` currently at pre-persistence baseline (zero D1 reads, `PracticeContentProvider` + `DocumentationProvider` both seed from fixture modules, `advisorPracticeProfile` hardcoded as Chrome identity); §6.11 path audit found **46 hardcoded `/advisor/` sites across 13 files** (vs Individual's 13 / 8 — ~3.5× the surface). **NEXT**: routing-only slice first (dispatcher branch + `/app/advisor` route, no data isolation) as a small proof-point before committing to the 46-site path audit or the persistence design pass. **PATH-FIX SLICE LANDED (§6.11 satisfied).** Following the routing-only slice (`ce6d8be`), this slice rewrote all mount-aware navigation across the 13 Advisor files via a shared `useBasePath(demoBase, appBase)` extracted to `src/contexts/AppIdentityContext.jsx`. Individual's `surfaces/individual/useBasePath.js` remains as a thin no-arg delegate calling `sharedUseBasePath('/individual', '/app/individual')` — its 10 consumers unchanged, zero behavior change. **Actual site count 48 across 13 files, correcting the earlier recorded 46**: the original grep pattern `/advisor/` (with trailing slash) missed the two `/advisor` root-path sites in `AdvisorSurface.jsx` (`:23` NAV_ITEMS Practice root + `:79` fallback Navigate) — both required rewriting. Verification: babel-parse all 15 touched files ✓, `npm run build` clean, smoke test all 7 Advisor demo routes + `/app/advisor` + Individual regression (`/individual/*` + `/app/individual/*`) all 200. Post-slice grep in `src/surfaces/advisor/` shows 17 remaining `/advisor` hits — every one is either the `useBasePath('/advisor', '/app/advisor')` call itself (16) or `ClientRoster.jsx:16`'s descriptive URL-semantics comment (1); zero hardcoded literals in navigation targets. **Still out of scope**: authenticated data isolation (`PracticeContentProvider` + `DocumentationProvider` still seed from fixture modules; Chrome identity still hardcoded to Morgan Walker), persistence design pass (no D1 tables for advisor entities). Advisor persistence arc: routing ✓, path-fix ✓, data-isolation OPEN, persistence design OPEN. **SCHEMA + SLIM SEED BANKED + APPLIED --REMOTE (this session, 2026-07-01 continuation).** Schema draft **RULED** (`6805c4a`) + amended review-cycle (`ef035d3`): 12 Q1-Q12 rulings applied, Q9 guardrail 8/8 inline, `doc.id` opaque UUID, `doc.updated` + `cohort.started` ISO 8601, gate mechanism ruled = **option (b) role gate** (writes accepted only for designated demo/staff advisor `person` rows, checked per-request against the owning person row; upgrade path to option (c) row-level allowlist when Q7 counsel answer lands). Full spec at `docs/advisor-persistence-schema-draft.md`. **Migration 0007 banked + applied --remote** (`0d2252a`): 8 tables — `client`, `client_session`, `client_note`, `practice_lesson`, `doc_category`, `doc`, `cohort`, `cohort_member` — with 8 indexes (every "my X" FK indexed from row one, D1 rows-read discipline). Local smoke gates: FK enforcement fires (`SQLITE_CONSTRAINT` on bogus owner), two-hop cascade holds (person → client → client_note). Q9/Parker invariants hard-commented in every table's DDL for drift prevention. **Migration 0008 slim seed banked + applied --remote** (`2acf0bb`): FT ruled slim scope — ONLY Morgan Walker's practice profile (`extensions.advisor: {practiceName, advisorTitle, practiceFocus, yearsActive}` overwriting 0005's partial shape via `json_set(coalesce(...), json(...))`) + 5 `practice_lesson` (pl-001..pl-005 preserved, `client's` apostrophe round-trips) + 2 `doc_category` + 4 `doc` (opaque UUID ids per FIX 2, ISO `updated` per FIX 3) + 2 `cohort` (`women's` apostrophe round-trips, `started` normalized first-of-month ISO). **FOUR client-side tables (`client` / `client_session` / `client_note` / `cohort_member`) DELIBERATELY EMPTY per FT ruling.** Rationale on the record: (1) clients enter D1 exclusively through the gated write endpoints when wire-surfaces lands — real-path testing exercising the Q4/Q7 role gate as designed; (2) fictional roster (c-001..c-009) stays in `src/data/clients.js` powering the public demo, no fixture-to-D1 drift surface; (3) sensitive-reading fixture content (private notes, session summaries, giving-plan narratives about fictional athletes) never enters D1 at all — even as fictional seed data. Full slim-scope amendment at `docs/advisor-persistence-schema-draft.md` §7 (drafted Client seed section preserved as SUPERSEDED). Remote apply proven parity with local (0007=17 commands / 0008=9 commands both sides; all 8 tables + indexes present; row counts match spec; Morgan's 4 profile fields parse; apostrophes round-trip). Observed: `person` count on remote is 5 vs local 4 — benign environment divergence (real magic-link sign-in prior to this ship, per the auth-slice (c) claim hook's fresh-person branch), NOT a migration side effect (neither 0007 nor 0008 has a `person` INSERT). **Arc state**: routing ✓, path-fix ✓, scoping ✓ (12 rulings), schema ✓ (RULED + amended), seed ✓ (slim), endpoints OPEN (gated per §6 role gate), provider bridge OPEN (`AuthenticatedAdvisorProvider` shape per Q3 team default), wire-surfaces OPEN. **Open Derek item**: Q7 processor-vs-joint-controller — the counsel-gated seam that unlocks `client` / `client_session` / `client_note` real-client writes; parallel to ruling E's Clause 6 (who-gave-to-whom view UNBUILT until subpoena posture set); the schema landed, the gate holds, real-people data waits behind Q7. Full scoping at `docs/advisor-persistence-scoping.md`. **WIRE-SURFACES SLICE 1 SHIPPED** (`7ca21e9`): authenticated Advisor READS live. `/api/me` widened with an owner-scoped `advisor` block for `type='advisor'` accounts — `practiceProfile` (from `person.extensions.advisor`), `practiceLessons`, `docCategories` with `docs` nested per-category, `cohorts` with `memberIds: []` per the Q7 gate (cohort_member is empty; population happens through the write path). Q11 single-fetch held: everything is one `/api/me` call, no per-entity fetches on advisor surface mount. `PracticeContentProvider` + `DocumentationProvider` gained optional `initialState` (matching `IntakeProvider`'s widening precedent); `initialState` is **FOLDED INTO `AdvisorSurface`'s OWN provider mounts** via `useOptionalAppIdentity()`, using `?? undefined` so null cleanly falls through to fixture defaults on the demo tree. **LESSON (standing rule for slice 2 and all future surface wiring)**: the first attempt wrapped an outer `AuthenticatedAdvisorProvider` around `<AdvisorSurface />` at the `/app/advisor` route in `App.jsx`. Silently shadowed by `AdvisorSurface`'s OWN inner `PracticeContentProvider` + `DocumentationProvider` (React resolves nearest-ancestor context) — every `usePracticeContent()` / `useDocumentation()` call site inside the surface still read the fixture-seeded inner providers, not the server-seeded outer wrapper. Caught in the review pass, fixed by fold-in, wrapper file deleted. **Standing rule**: seed the surface's OWN provider instances (via prop-threading from a hook that reads server data), NEVER wrap a parallel provider outside a component that mounts its own — nearest-ancestor resolution silently defeats the outer seed. Chrome identity swap live: `AdvisorSurface` reads `identity.displayName` + `identity.advisor.practiceProfile.advisorTitle` from `AppIdentityContext`, falls back to `advisorPracticeProfile.advisorName`/`.advisorTitle` fixture on the public demo tree. **End-to-end verified** via local signed-cookie Morgan smoke: constructed `better-auth.session_token` cookie by HMAC-SHA256(BETTER_AUTH_SECRET, plain-session-token) base64 → URL-encoded → curl `/api/me` → returned full slim-scope advisor payload matching spec (4 practiceProfile fields with correct values, 5 practice_lesson rows pl-001..pl-005, 2 doc_categories Onboarding+Working notes with 4 total docs, 2 cohorts coh-001+coh-002 with all `memberIds: []`); LOCAL D1 baseline restored exactly after (`auth_user=1`, `session=2`, Morgan's `auth_user_id=NULL`, smoke rows absent). Cookie construction was mechanical (no signature-internal guessing) once better-auth's `makeSignature` in `better-call/dist/crypto.cjs` confirmed standard HMAC-SHA256 + base64 shape. **CORRECTION to prior `person=5` remote speculation**: the 5th remote row is `26fae94e-97a5-4221-9976-40e1b1b2e8ff` (Marcus Thompson, `invite_email=NULL`) — the RESTORED standalone Marcus from the un-reconciliation slice this session (see Individual row item 5), NOT a real magic-link sign-in fresh-person as the prior schema+seed bank tentatively suggested. Remote at expected state: FT's reconciled row + 3 demo roster (Jordan/Morgan/Reese) + restored standalone Marcus = 5. **SLICE 2 QUEUED** (fixture-import isolation per the fold-in rule above): 7 leak files — `PracticeHome.jsx`, `PracticeSettings.jsx`, `Pipeline.jsx`, `ClientRoster.jsx`, `ClientWorkspace.jsx`, `CohortSpace.jsx`, `CohortDetail.jsx` — all still directly import `advisorPracticeProfile` / `clients` / `cohorts` from fixture and will leak demo data on authenticated mount even with slice 1 providers seeded. 3 files PLATFORM-SAFE per Q10 (`CurriculumLibrary.jsx`, `LessonDetail.jsx`, `LessonEditor.jsx` — base lesson library only, stays fixture-sourced by ruling). Slice 2 plan: **new `CohortsProvider` + `useCohorts` hook** for cohorts (fold-in seeded from `identity.advisor.cohorts`), **gated `useClients` hook** for the client roster returning `[]` until Q7 unlocks (consumers must handle empty-list gracefully — likely a "Clients will appear here once the practice signs in with real accounts" placeholder), practice-profile reads shift from `advisorPracticeProfile` fixture to `identity.advisor.practiceProfile` via `useOptionalAppIdentity()`. **Arc state**: routing ✓, path-fix ✓, scoping ✓, schema ✓, seed ✓, **authenticated reads ✓ (slice 1)**, fixture-isolation OPEN (slice 2), write endpoints OPEN (gated per §6 role gate; Q7 counsel-gated). **Doc-pass note**: local `.dev.vars` currently holds the shipped `.dev.vars.example` placeholder as `BETTER_AUTH_SECRET` (30 chars, literal `paste-your-openssl-secret-here`) — auth works fine for local dev but future doc pass should surface this so local security habits match prod. **WIRE-SURFACES SLICE 2 SHIPPED** (`8c3e4f7`): fixture-import isolation complete across the 7 leak files identified in the slice-1 isolation map (`PracticeHome`, `PracticeSettings`, `Pipeline`, `ClientRoster`, `ClientWorkspace`, `CohortSpace`, `CohortDetail`). New `CohortsProvider` (`src/contexts/CohortsContext.jsx`) fold-in seeded from `identity.advisor.cohorts` on the authenticated tree, fixture on demo; new **Q7-gated `ClientsProvider`** (`src/contexts/ClientsContext.jsx`) with authenticated `initialState` ALWAYS `[]` (no server client rows land until the write-endpoint role gate opens per schema-draft §6), fixture on demo. Both providers folded into `AdvisorSurface.jsx`'s own provider stack (the standing rule from slice 1 — seed the surface's OWN providers, never wrap outside a component that mounts its own). Practice-profile reads (`PracticeHome`, `PracticeSettings`, `Pipeline`) shifted from direct `advisorPracticeProfile` fixture reads to `useOptionalAppIdentity().identity.advisor.practiceProfile` with fixture fallback per the Chrome pattern; `advisorName` resolves to `identity.displayName` (redundant `advisorName` field NOT stored in `extensions.advisor`). **Import audit** proves zero `clients`/`cohorts` data-array imports remain across the 7 files — the ONLY remaining imports from `../../data/clients.js` are `formatSessionDate` (util in 5 files), `stages` (shared taxonomy in 3 files), and `advisorPracticeProfile` (demo-fallback only in 3 files, always guarded by `?? advisorPracticeProfile.X`). **Empty-state copy** "Your clients will appear here." (§7 voice: quiet register, no exclamation, no product-internal words like pilot/gate/Q7) rendered at two sites — PracticeHome upcoming-sessions Card + ClientRoster list section — both wrapped in `<p>` with `fontStyle: italic` `color: var(--sh-text-muted)`. **Stat surfaces show honest zeros** per the affordance-honesty precedent: PracticeHome stage-counts (`clients.filter(c=>c.stage===S).length` = 0), PracticeHome pipeline footer (`0 content items active across 0 clients`), Pipeline coverage rows + footer, ClientRoster's `Showing 0 of 0 clients` counter. **Pipeline `pipelineDefaults` disposition**: fixture ships hard-coded per-content-type counts derived from the 9-client fixture roster; on the authenticated tree with `clientCount === 0` those become false statements, so Pipeline zeroes them at mount (`Object.fromEntries(Object.entries(pipelineDefaults).map(([k, v]) => [k, { ...v, clientsOnDefault: 0, overrides: 0 }]))`). **Follow-up FLAGGED** for when the client write path (Q7) opens: re-derive `clientsOnDefault` + `overrides` per content type from `clients[].pipeline[type].source === 'default' | 'override'` counts, dropping the fixture pipelineDefaults counts entirely. **DEFENSIVE SEAM NOTED for a future hardening pass**: the `ClientsProvider` gate in `AdvisorSurface.jsx` keys on presence-of-`advisorData` (`initialState={advisorData ? [] : undefined}`), NOT presence-of-session. An authenticated advisor whose `/api/me` response somehow lacked the `advisor` block would fixture-seed instead of gating to empty — structurally shouldn't occur (`AppDispatcher` routes by `person.type`, `/api/me` unconditionally includes the `advisor` block when `person.type === 'advisor'`) but the seam is named. Safer future gate: key on `identity?.type === 'advisor'` directly instead of the derived `advisorData`. Demo tree byte-identical (12-curl smoke green including detail routes `/advisor/clients/c-001` + `/advisor/cohorts/coh-001` that prove `useClients` + `useCohorts` correctly resolve fixture data on demo). Morgan signed-cookie smoke re-run: same surgery + cookie construction as slice 1, `/api/me` response confirmed the slice-1 payload contract still holds (type=advisor, displayName=Morgan Walker, 4-field practiceProfile, 5 practice_lessons pl-001..pl-005, 2 doc_categories Onboarding+Working notes with 4 total docs, 2 cohorts coh-001+coh-002, all `memberIds: []` per Q7 gate); LOCAL D1 baseline restored exactly after (`auth_user=1, session=2, Morgan.auth_user_id=NULL`). **Arc state**: routing ✓, path-fix ✓, scoping ✓, schema ✓, seed ✓, authenticated reads ✓, fixture isolation ✓ — **REMAINING**: write endpoints (gated per §6 role gate, waiting on Q7 processor-vs-joint-controller counsel) + provider write-through (`add`/`update`/`remove` on `PracticeContentProvider` + `DocumentationProvider` currently local-state-only; extend to POST-through to per-entity endpoints — new endpoints `POST/PUT /api/practice-content`, `POST/PUT /api/documentation`, `POST /api/clients` (gated), etc.). Once write endpoints land, Advisor reaches Individual's reference-pattern parity (read + write persistence end-to-end). **PARKED DEFECT** (live-confirmed 2026-07-01, FT manual browser check): an authenticated INDIVIDUAL-type user (e.g. FT signed in against her Individual account) navigating directly to `/app/advisor` sees the fixture-seeded Advisor surface instead of being redirected to her own `/app/individual` surface. **Root cause**: no route-level type guard on `/app/advisor`. `AppDispatcher` only fires on `/app` itself (its index route); direct navigation to `/app/advisor` bypasses the dispatcher entirely. Combined with the **named defensive seam** from slice 2 (`ClientsProvider` gate keys on presence-of-`advisorData`, not on `identity.type === 'advisor'` — a null `advisor` block on an authenticated non-advisor session falls through to fixture seeding on an authenticated route). Individual-side has a mirror-image gap: an authenticated advisor navigating directly to `/app/individual` would see the fixture-seeded Individual surface. **Fix scheduled** for the WRITE-ENDPOINTS session (end of Advisor build): type guard on BOTH `/app/advisor` and `/app/individual` route elements at `App.jsx` (element wraps a type-check that redirects to `/app` if `identity.type` does not match the surface), closing the seam symmetrically for both surfaces. Not shipping the fix earlier because (a) the write-endpoints session is the natural boundary where both surfaces get a hardening pass together, and (b) the defect is a UX confusion — not a data-leak (the fixture-seeded surface shows fictional demo data, no real per-user data crosses the boundary). **PARKED DEFECT CLOSED** (`e8683f3`, this session): `RequireType` guard component (`AppIdentityContext.jsx`, **guard-wrapper-not-provider** per the slice-1 shadowing-lesson distinction — reads the context AppShell already provides one level up, passes children through verbatim; no parallel provider mounted, so nearest-ancestor resolution has nothing to shadow) wraps both `/app/advisor` (requires `'advisor'`) and `/app/individual` (requires `'individual'`) symmetrically at App.jsx. Waits on the context's `status` field: `'loading'` → renders AppShell's exact "Checking your session…" panel (**defensive-unreachable-today** since AppShell blocks `<Outlet />` during load, but kept as documented safety net if that gating logic ever changes — a premature Navigate on an in-flight `/api/me` would bounce valid users); `'ready'` + `identity?.type !== type` → `<Navigate to="/app" replace />` so `AppDispatcher` re-routes to the correct surface. Unauthenticated case remains handled upstream by AppShell (redirects to `/signin` before children mount) — RequireType only distinguishes the wrong-type case. **Verified**: demo trees byte-identical (guards live only under `/app/*`, all 13 SPA routes 200 including `/individual/*` + `/advisor/*` demo trees); Morgan signed-cookie smoke passed the correct-type path (advisor session's `/api/me` returned `person.type: "advisor"` + full advisor block with 4-field practiceProfile, 5 practice_lessons, 2 doc_categories with 4 docs, 2 cohorts with `memberIds: []` per Q7 gate → RequireType's `identity?.type !== type` check evaluated false → children render); exact baseline restore (`au_ct=1, sess_ct=2, morgan_link=NULL` pre = post). The **wrong-type bounce is client-side React beyond curl's reach** — FT's live browser check after deploy is the definitive verification (individual account → direct-nav `/app/advisor` → expect immediate bounce to `/app/individual`; the mirror case for an advisor account hitting `/app/individual` shares the identical guard logic, so if one works the other does too). **RULED — single-type identity is PERMANENT** (FT, 2026-07-02): one `auth_user` → one `person` → **exactly one `type`** (`individual` | `advisor` | `enterprise`), in production, always. No multi-role identity, no role picker, no multi-role scoping pass owed. Real persons holding multiple roles use **distinct accounts / emails**. Testing across surfaces uses **plus-addressing** (`name+advisor@…`, `name+enterprise@…`) — distinct `auth_user` rows sharing one real inbox. The type guard, dispatcher single-redirect, and single-block `/api/me` shape are the **ruled model, not provisional** — future work does not need to leave seams open for a hypothetical multi-role future. |
| **Individual** | **Rework UNDERWAY (thawed this session) — Tier 1 COMPLETE; Tier 3 contained-structural phase COMPLETE (all 3 paused-surface folds closed: #12 + #63 + #116; CohortView unified rewire done); remaining = design-phase decisions only. 5.8 giving-flow design rulings LOCKED this session (`39bcaf0`, docs-only — full record at `docs/5.8-giving-flow-scoping.md`); persistence scoping pass COMPLETE (`d7bfe70`, docs-only — three strands at `docs/persistence-scoping-pass.md`); schema draft RULED A-F and now FULLY FINALIZED — ruling E (deletion / retention) RESOLVED this session on research synthesis (`4b9d1b5` ruling-E doc + `802a9aa` schema-draft E-resolved flip + this commit CLAUDE.md bank, all docs-only; own doc at `docs/ruling-e-deletion-retention.md`). Build UNBLOCKED except two isolated counsel-gated seams (charitable-floor; subpoena posture).** 15 surface files (Discover, GPSReveal, GivingModeler, Plan, Positioning, Privacy, Team, History, Learn, CohortView, Letter, Questions, Feedback, GiveScreen, IndividualSurface). v0.6.1 baseline confirmed building clean before first edit. The structural rework began this session; Individual is **no longer untouched-frozen**. **Disposition framing preserved**: the 15 surfaces largely stay; architecture / components / data-flow get restructured — adopting Cluster I primitives (`DataTable`, `MessageHistoryCard`, `Tag` `tracking='loose'` variant, Enterprise-scoped `SegmentedControl`) and aligning to `src/data/unified/`. This is **REWORK (revise the existing surface), NOT REBUILD (discard to zero)**. **Escape hatch exercised**: the scoping pass produced founder/team rulings on 7 escalations (5.1–5.7) + 2 parked (5.8–5.9); scope/direction did NOT pivot the frame — structural stands as primary. **Why no QA-arc audit was run**: auditing frozen code that's about to be restructured would invalidate findings; Enterprise / Advisor / Operations got audits because they were live-and-building. Full plan: `docs/individual-rework-scoping.md`. **Tier 1 COMPLETE** (`44ffb9e`) — correctness/safety items: **5.1 sector-language athletes-only sweep** (investigation found 3 user-facing sites; scoping under-counted at 1): `Positioning.jsx:10` body changed from `"Athletes, musicians, entertainers, creators — people whose careers..."` to `"Athletes — people whose careers..."` (multi-sector list dropped, construction preserved); `lessonsData.js:44` lesson body ending `"...specialize in high-earning professionals across sports, music, entertainment, and creator industries."` → `"...in athletics."` (multi-sector clause dropped); **`Feedback.jsx` Q0 "What world are you in?" (Sports / Music / Entertainment / Creator) DROPPED entirely** — Phase-1 athletes-only makes the sector self-ID meaningless; plus 3 downstream `world` refs cleaned for a complete drop (`buildExport` "World:" line removed, filter exclusion list trimmed, submission payload `world` field removed); QUESTIONS array count 12 → 11 + comment updated. `orgsData.js:12` ("Every Child an Artist NYC" + program names "Teaching Artist Residency" / "In-School Music") LEFT untouched — **not-a-violation, #89 INTENTIONAL-PRECISION precedent**: §7's names-verbatim-from-records guard protects proper-noun org/program names; they aren't platform sector-language. **5.3 feedback endpoint de-hardcode**: `Feedback.jsx:7` → `const FORM_ENDPOINT = import.meta.env.VITE_FEEDBACK_ENDPOINT ?? '<literal>';` — **Option B (env + literal fallback)** chosen over pure-env to avoid a Cloudflare deploy-dashboard dependency; follows the `import.meta.env` pattern already established in Operations (`sourceAccents.js:31`, `OperationsSurface.jsx:146`). Literal preserved as the `??` safety net, no longer a bare assignment. **DEPLOY FOLLOW-UP (parked, founder-initiated)**: set `VITE_FEEDBACK_ENDPOINT` in Cloudflare Pages env-vars dashboard when ready to override the literal; until then the literal ships unchanged. **Tier 2 settled this session, no code**: **5.2** keep founder letter in Individual (cross-interface entry-rationale parked — letter for Individual; open question for Advisor/Enterprise/Operations; intro-video one option); **5.4** "nav for recurring, CTA for contextual" IA principle (Give/Feedback/Cohort stay CTA-summoned, no nav addition); **5.5** GivingModeler stays a COMPONENT (embedded in Plan, no standalone route). **Tier 3 #12 WARNING-PALETTE FOLD COMPLETE** (`cd1a518`) — first Tier 3 build slice: added 3 tokens `--sh-warning-bg` / `--sh-warning-text` / `--sh-warning-border` to `tokens.css`; swapped Tag.jsx `colorSchemes.warning` off hex literals (3 existing `<Tag tone="warning">` consumers at Team:338 / :380 / :414 byte-unchanged); **token-swapped all hand-rolled Team.jsx warning sites — landed at 10 total, NOT the 4 the scoping anticipated** (Team:127 count-badge State-B + Team:193 / :211 / :277 / :320 / :361 / :400 past-due literals **surfaced via grep during the slice, scoping under-counted**). Zero warning hex literals remain outside `tokens.css` across `src/`. Team:127 carries the one intended visual shift (`#F8D7CC` → `#FCEAE0` canonical convergence; both pale warning-pinks); the other 9 sites are exact-value swaps with zero visual delta. **Tier 3 #63 borderRadius fold COMPLETE** (`913f89a`) — second paused-surface fold closed: `GiveScreen.jsx:204` + `GivingModeler.jsx:290` `borderRadius: '6px'` → `var(--sh-radius-md)` (token === 6px confirmed, zero visual change). Scoping count held — 2 anticipated, 2 actual. **Tier 3 CURRENT_USER alignment COMPLETE** (`a3f3c5e`, 5.6): `IndividualSurface.jsx` Chrome props `userName` / `userRole` sourced from `individualProfile` (`name` + `worldLabel`) instead of hardcoded `"Marcus Thompson"` / `"Member · Athletics"` literals; "Member · " prefix kept as call-site string (no single-consumer `roleLabel` field per #47 / QA-051 premature-abstraction lens); restore-button copy at :523 left literal (descriptive UI content, same class as orgsData proper-noun ruling); **NO unified import** — Individual stays raw-aligned, the unified-vs-raw design call deferred to the CohortView slice. 1 site anticipated, 1 actual. **3 of 3 paused-surface folds closed** (#12 + #63 + #116) — **the paused-surface fold list is COMPLETE**. **Tier 3 CohortView unified-data-model adoption COMPLETE** (`eab1c71`) — **FIRST structural Tier 3 slice; FIRST Individual file to read from `src/data/unified/`**. Rewired 5 data lookups from raw `cohorts.js` / `clients.js` to `unified.cohorts` / `unified.programParticipations` / `unified.persons`. **Render byte-identical**: node-parity verified — same cohort name ("Youth and school athletics access"), same self themes (3 themes), same other-member names (Isaiah Coleman, Naomi Pierce), same shared-interest counts (`youth-sports-access:2`, `education-access:2`). **Rulings**: **D1** call-site identity bridge — `const currentMemberId = \`p-advisor-${individualProfile.id}\`;` crosses the deferred same-person dedup gap per CLAUDE.md §4 (the two unified personas `p-individual-c-001` + `p-advisor-c-001` are NOT joined; the cohort + giving-plan data lives under the advisor namespace; documented inline as the line to revisit when dedup lands); **D2** `simulatedMemberSignals` kept raw, prefix-strip translation at the lookup boundary (`simulatedMemberSignals[om.id.replace('p-advisor-', '')]`); **D3 / 5.7** `CohortMemberContext` DEFERRED, stays Individual-local — pure session state with no fixture dependencies; moving it to unified gated on the broader "unified as live store?" / persistence-pilot question (this slice consciously left it Individual-local with an inline 5.7-DEFERRED marker, NOT just parked). Contained to `CohortView.jsx` (no cascade — the 4 raw fixtures it read have zero other Individual consumers; Advisor still reads them raw, out of scope). **Architectural shift**: the unified-vs-raw boundary now runs **through** Individual, not around it. CohortView reads unified; the other 14 Individual files still read raw. Future Individual structural work has a precedent for which side of that boundary to choose. **Tier 3 #116 InteractiveCard CLOSED as documented-debt** (Option (b), **ruled not refactored**) — investigation confirmed exactly 3 interactive-Card consumers (`IndividualSurface.jsx:258` + `:447` + `Learn.jsx:86`), all uniform `<Card interactive onClick={fn}>` shape. Card is a **50-consumer shared primitive** with **only the 3 Individual sites** on the interactive branch (tree-wide grep verified zero non-Individual consumers of the interactive branch). **Declined extraction**: modifying shared `Card.jsx` to resolve a 3-consumer trivial per-mount cost (an always-allocated `useState(false)` for `hovered` consumed only when `isClickable`) is upside-down cost/benefit. Same lens as the #48 SegmentedControl Enterprise-scoping ruling (don't refactor a shared primitive's API in a single-surface slice without cross-surface justification) + the #47 / #57 premature-abstraction-at-current-scale lens. Sibling disposition: the 12-pill migration was also closed by **ruling-not-build** (invalidated-and-skipped by investigation rather than refactored). The `Card.jsx:4-6` comment already documents the finding + consumer set; this slice formalizes it as the FINAL ruling. **Revisit only if** Card's interactive branch becomes heavily cross-surface, OR `Card.jsx` is being refactored for another reason anyway. **Tier 3 REMAINING — design-phase decisions only** (no mechanical rewires left): ~~12-pill Tag `tracking='loose'` migration~~ — **INVALIDATED & SKIPPED** per the `b0f0e0c` scoping correction; **further unified-data-model adoption** if pursued — the gifts / ID-namespacing / context decisions (Section 3 divergences #2-5 in the scoping doc); **5.7** CohortMemberContext location (explicitly DEFERRED and documented inline at the CohortView call site, gated on unified-as-live-store); **5.8** Plan ↔ GivingModeler ↔ GiveScreen flow — **design-LOCKED this session** (`docs/5.8-giving-flow-scoping.md`; full rulings + Parker hard lines + carried debt + NEXT pointer banked below); **5.9** IntakeContext scope split (intake-only vs activity, or one bag?) — folds into the 5.8 schema pass since the gated `intake-gifts-join` question is now answered YES. **All remaining items are product/architecture questions, not mechanical rewires.** **Scoping-count discipline log**: Tier 3 slice tally so far — **12-pill correction** (`b0f0e0c`: 12 → 0 invalidated), **#12 fold** (`cd1a518`: 4 → 10 expanded), **#63 fold** (`913f89a`: 2 → 2 held), **CURRENT_USER** (`a3f3c5e`: 1 → 1 held), **CohortView unified rewire** (`eab1c71`: 5 lookups anticipated → 5 actual; expansion was decision-surface — D1/D2/D3 rulings — not site-surface; the first structural slice still held its count), **#116 ruling** (3 consumers anticipated → 3 actual; closed by disposition not build — second paused/Tier-3 item closed by ruling-not-build, sibling to the 12-pill invalidation). **Tier 3 counts are FLOORS, not ceilings** — contained slices and the first structural slice all held; larger pattern-matching slices (12-pill, #12) moved both directions; ruling slices (#116, 12-pill) closed without site change. Plan investigation time per slice; don't promise the scoping count as the final count until the slice's own grep sweep is done. **5.8 GIVING-FLOW DESIGN RULINGS LOCKED** (`39bcaf0`, docs-only, full record at `docs/5.8-giving-flow-scoping.md` — two read-only investigations preceded the rulings: 5.8-flow shape + the three-shape consolidation blast-radius sweep). **(1) Canonical entity**: Gifts-first ratified; canonical Gift = unified-shape structural spine (explicit `giverPersonId` + real `recipientOrgId` + `recipientOrgName` + ISO `date` + `sourceSurface`) MERGED WITH session-shape display reality (`notes` retained); GiveScreen session writes FEED the canonical store (resolves the gated `intake-gifts-join` question: YES); the three coexisting gift shapes (`individualProfile.gifts` seed / `IntakeContext.state.gifts` session / `unified.gifts`) consolidate into one canonical Gift. **(2) Modeler scenarios**: persisted + lightly versioned as a net-new entity (snapshot-not-history floor — enough for modeled-vs-actual comparison over time). **(3) Flow directionality**: any-entry graph; "Plan" is a lens, not a parent; surfaces read/write shared Gift + Scenario entities; GiveScreen-first entry IS in v1 scope. **Parker hard lines carried into the schema pass** (must survive into any field-set draft): canonical Gift is a donor-DECLARED record with at most an "exported to CPA" marker — **NO payment/execution/settlement/transfer-lifecycle field, ever** (confirmed: none of the 26 fields across the three current shapes carries such state; the invariant now needs schema-level enforcement once gifts persist to accounts); GivingModeler stays a structural canvas the donor builds — never a recommender/optimizer/rank engine (scenario shape must not introduce a scoring/ranking/"suggested allocation" axis). **Carried debt** (persistence/schema pass to absorb, not fixed yet): **(a)** budget-literal mismatch — `individualProfile.js:27` `'$1K–$10K'` (no spaces) vs `intakeData.js:30` BUDGETS array + `GivingModeler.jsx:7` `BUDGET_TO_DEFAULT` keys both `'$1K – $10K'` (spaces) → dead lookup for the canonical demo user, falls through to `5000` default; **(b)** `Date.now()` gift ids at `GiveScreen.jsx:63` → `crypto.randomUUID()` per Enterprise QA #97 precedent, plus submit not disabled in-flight; **(c)** date-format unification to ISO — `parseGiftDate` requires `'Month D, YYYY'` and would null on both session-born `'Apr 23'` and ISO `'2026-03-12'`. **NEXT (still 5.8, planning session)**: canonical Gift field set (recipient-FK seam to watch); scenario-versioning spec (snapshot floor); entity/relationship sketch (surfaces the `GiftRow` extract-or-not question for FT; pre-answers `intake-gifts-join` + `individualProfile`-raw-vs-unified; opens the persistence scoping pass). **THEN persistence scoping pass — 3 strands**: D1 + Workers + unified / hosted-auth diligence / live 4/30 data-protection frame. **PERSISTENCE SCOPING PASS COMPLETE** (`d7bfe70`, docs-only, full record at `docs/persistence-scoping-pass.md` — read-only throughout, no code). Three strands designed together. **Strand 1 (architecture)**: green-field finding — `src/data/unified/` is a pure build-time projection with ZERO write path (no setter, no observer; `unified.gifts` has zero surface readers); no `wrangler` config, no `functions/` or `workers/` dir, no auth/DB deps in `package.json`; the app is a pure static SPA on CF Pages. The live account-backed store is a NEW layer BESIDE unified, not an edit to it. Open rulings for the schema draft: (a) live-store REPLACES vs COEXISTS; (b) seed-data migration story; (c) `sourceSurface:'individual'` partition semantics. Recipient-FK reality refines 5.8 (FK target exists only for 17 individual-catalog orgs; enterprise gifts produce zero Org records). No identity layer today — current user = hardcoded Marcus fixture, not authenticated session. **Strand 2 (auth)**: RECOMMENDATION — `better-auth`, self-hosted in the project's own D1, magic-link EMAIL-ONLY (no SMS), Cloudflare Email Service as send pipe with Resend as named swappable fallback. Rejected hosted SaaS (Clerk / Auth0 / Stytch) on identity-custody grounds (not cost — all free at pilot scale; they hold user identity on their infra, colliding with the data-protection frame); rejected roll-your-own `jose`+JWT on security-burden grounds (build-your-own break-even ~100k MAU). Caveats carried to build: (a) CF Email Service is BETA — abstract sender behind swappable interface (Resend GA is fallback); (b) `better-auth`+D1+Workers sharp edges — one auth/D1 instance per request at middleware top (singleton silently breaks because D1 binding changes per invocation); disable `cookieCache` per session-refresh bug #4203 (trade a D1 read per session check for correctness); (c) 10ms free-tier Worker CPU + indexed session-check read from day one. Resolves `giverPersonId` = FK from auth-user table → Person, both in the project's own D1. **Strand 3 (data-protection — the 4/30 frame now live against the concrete architecture)**: a gift record (`giverPersonId` → `recipientOrgId` + amount + cause) for an athlete / creator / public figure can reveal political / religious / cause alignment + net-worth signal; whose giving is stored changes the stakes. **L1 schema constraints** — identity SEPARABLE from giving data (gifts keyed by opaque person-id; auth-table join as guarded chokepoint); recipient + cause is the SENSITIVE payload (not amount); email lives in two systems (`better-auth` user table + CF Email send logs) — name it. **L2 legal — DEREK NOW CRITICAL-PATH** (3 live questions): controller identity under PBC + captive 501(c)(3); regime scope (giving data touching cause / religion / politics is "special category" under GDPR + post-2024 US state acts); subpoena / legal-process posture (decided BEFORE storing data because it can change a Layer 1 architecture decision); **scoping-for-Derek, not legal advice**. **L3 ops** — magic-link removes the password-breach + credential-stuffing surface but makes the email inbox the account-takeover single point (conscious acceptance); `better-auth` sharp edges are also security items (session bug = security event); D1 production access logged + minimal from day one; D1 Time Travel (30-day restore) is a privacy surface — deleted user data persists in restore points; deletion commitment must account for it. **L4 governance** — 10%-gross commitment + no-third-party-ad posture already answer the biggest governance question (no data monetization, in the operating agreement); retention / deletion policy must exist BEFORE pilot, kept compatible with Parker's no-lifecycle-field rule (a deletable Gift must not reintroduce a lifecycle field by the back door); individual-pushes-consent extends to data. **PERSISTENCE SCHEMA DRAFT RULED A-F** (`0eef5e1`, docs-only, full record at `docs/persistence-schema-draft.md`) — the **rule** artifact in the scope → rule → build sequence; **FINALIZED EXCEPT ruling E** (deletion / retention — Derek-gated). FT rulings verbatim: **A = REPLACE** (live D1 store is the single source of truth for user-authored entities — Person / Gift / Scenario / intake; unified build-time projection serves seed / demo only); **B = person-is-identity CONFIRMED** (the `person` table IS the user identity; `individualProfile.js` becomes seed data loaded into `person` rows, not a parallel truth); **C = `person.id`-opaque separation SUFFICIENT** at pilot (no extra indirection layer; `gift` table alone does not reveal giver identity without the `person` → `auth_user` join — the guarded chokepoint per strand-3 L1); **D = `org` becomes a D1 TABLE** (not a build-time projection; `gift.recipient_org_id` is a real hard FK with referential integrity; the 17 seed orgs migrate as real rows — same seam as Marcus's gifts); **E = RESOLVED** (deletion / retention accepted on research synthesis this session — full record `docs/ruling-e-deletion-retention.md`; banked as `4b9d1b5`. Two-phase soft-then-hard deletion enacted at the **`person` boundary** — NOT a per-Gift status column; Parker no-`deleted_at`-as-status invariant intact, deletion cascades from person to gift / scenario; anonymize-not-orphan for any structurally-retained fact; minimal justified retention window; backup / restore re-applies pending deletions; deletion ledger keyed by opaque marker; subpoena posture = identity stays separable, **who-gave-to-whom view stays UNBUILT until posture set**. Two counsel-gated seams remain isolated — Clause 3 charitable-retention-floor confirmation; Clause 6 subpoena posture — and do NOT block build); **F = Marcus seed-migration CONFIRMED** (Marcus = first real account through real auth; the demo "load Marcus" path IS the account path, exercising the real identity model; Marcus gets a real `auth_user` + `person` row; his 3 seed gifts become real `gift` rows owned by his `person.id`). **Tables (replace-model, all under the live D1 store)**: `auth_user` (better-auth-owned — ONLY place email lives in the data model besides CF Email send logs); `person` (opaque id, nullable `auth_user_id` for seed / demo persons with no account, `source_surface`, `extensions`); `gift` (`giver_person_id` FK, `recipient_org_id` FK nullable by design, `recipient_org_name` always present, `exported_to_cpa` = Parker's ONLY lifecycle-adjacent field — no other lifecycle / status / settlement column, ever); `scenario` (`owner_person_id` FK, `inputs` = 6 modeler knobs, `derived_at_snapshot` cached + nullable, NO rank / score field per Parker Modeler guardrail); `org` (durable id from the 17-catalog ids, now a D1 table per ruling D). **Indexing from migration 1** on every "my X" FK — `gift.giver_person_id`, `gift.recipient_org_id`, `scenario.owner_person_id`, `person.auth_user_id`, plus the session-check read (D1 bills rows-read; unindexed full-table scans = surprise bills). **Folded-item resolutions** (gated items, now resolved): `intake-gifts-join` = YES (5.8); `individualProfile`-raw-vs-unified resolves toward UNIFIED-AS-LIVE-STORE (account-backed Person IS the user identity; `individualProfile` becomes demo-seed, not parallel truth); CohortMemberContext / 5.7 was gated on ruling A (now resolved REPLACE — revisit); ID-namespacing folds into the `sourceSurface` partition question. **NEXT**: schema **IS finalized** (E resolved on research, `802a9aa` flipped the draft from "FINALIZED EXCEPT E" → "FULLY FINALIZED"); FT has **AUTHORIZED build**. Build sequence unchanged: schema / migrations → auth → wire surfaces. Two counsel-gated seams remain isolated (Clause 3 charitable-retention-floor; Clause 6 subpoena posture / who-gave-to-whom capability stays UNBUILT until posture set) and do NOT block schema / migrations / auth / surface wiring — they are confirmed by counsel IN PARALLEL with the build. **FOUNDER ACTION**: Derek (or alternate counsel) is **NO LONGER a hard gate** on the persistence build — the 3 Layer-2 legal questions (controller identity under PBC + 501c3, special-category regime scope, subpoena posture) PLUS the two ruling-E counsel-gated seams (charitable-floor, subpoena posture) are confirmed IN PARALLEL; they isolate to specific seams and do not block schema / migrations / auth / surface wiring. Build de-risked from single-point-of-failure on counsel responsiveness. Derek brief + email were prepared this session and are FT's to send. **DISCOVER DESIGN PASS — 2026-06-24** (design-only, NOT built; captured for FT synthesis, full record in FT Google Drive doc "Discover Design Pass — Program Depth, Support Copy, Category Taxonomy" dated 2026-06-24). Three items: **(1) program descriptions** — GuideStar-style depth, programs become structured `{name, description?, impact?}` (Discover currently renders a flat string list per org); public-sourced from org websites + IRS 990 Part III, **surfaced not authored** (Path B), blank-state-first, manual-seed the 17 catalog orgs for pilot; folds into ruling D (org table in D1) — programs shape becomes part of the `org.programs` column / extensions. **AI-drafted descriptions explicitly OUT for now** per FT defer-to-Parker; AI-authorship parked as a future decision. **(2) support copy** — the generic "When you support this organization" block at `Discover.jsx:470-485` over-promises a post-gift relationship loop the product can't yet deliver: **REMOVE NOW**; the real version returns post-gift / persistence arc, **org-voice only** when it returns. **(3) category taxonomy** — map to existing sector terms: rename **"Emerging" → "Grassroots"**; redefine **"Community-Rooted"** (= community-based / led) by **GOVERNANCE not scale**; keep **"Established"** (verify it reads as a **kind** not a **tier**); one-sentence in-product definitions; **kinds-not-stages**, no evaluative / ranking implication (Path B + no-scoring guardrail). Spec-able NOW, independent of persistence — touches `CAT_META` (`orgsData.js:34-47`) labels + descs only. **PARKED — STRATEGIC**: FT wants future **AI-drafted org descriptions** + broader **AI-in-platform integration**; its own deliberate future discussion, not foreclosed; defer-to-Parker holds for now. **PERSISTENCE BUILD STARTED — migration 0001** (`749ec83`, first backend infrastructure the app carries). Schema-only sub-slice, LOCAL-VERIFIED then pushed: `wrangler` devDep added, `wrangler.toml` (D1 binding `DB`, database_id = PLACEHOLDER pending FT-run `wrangler d1 create stewardhouse-pilot`), `migrations/0001_initial.sql`. Five tables per schema draft §3: `auth_user` + better-auth companions (`session` / `account` / `verification`), `person`, `org`, `gift`, `scenario`. All 8 "my X" FK indexes from migration 1 (D1 bills rows-read). Ruling-E deletion shape built: `ON DELETE CASCADE` person→gift/scenario (person-boundary), `ON DELETE SET NULL` gift→org (nullable-by-design); `person.soft_deleted_at` + `deletion_state` columns present (Clause-3 hard-delete interval NOT baked — counsel-gated config). Parker invariants hardened into DDL as comments: `gift` has no lifecycle column beyond `exported_to_cpa`; `scenario` has no rank/score column. Counsel-gated UNBUILT (commented in SQL): Clause 3 charitable-floor literal, Clause 5 deletion ledger, Clause 6 giver↔recipient join view. Conventions: auth timestamps `INTEGER` epoch-ms (better-auth default), non-auth timestamps `TEXT` ISO; `auth_user` omits `name`/`image` (magic-link; display identity on `person.display_name` per ruling B). **NEXT**: FT-run cloud step (`wrangler login` → `d1 create stewardhouse-pilot` → real `database_id` swap → `migrations apply --remote`), THEN seed sub-slice (0002: Marcus person + 17 orgs + 3 gifts, `recipient_org_id` all-null by design, ISO date + budget-literal normalization), THEN auth wiring, THEN surface read/write. **SEED MIGRATION 0002** (`90e45b7`, local-verified then pushed) — `migrations/0002_seed.sql`, individual-side seed only (rulings A + F). 21 rows: 17 orgs (`'org-1'..'org-17'` from `orgsData.js`, `source_surface='individual'`, `causes` + `extensions` as JSON `TEXT`) + 1 person (Marcus, fresh opaque UUID id, `auth_user_id` NULL per option B — binds to a real `auth_user` on first magic-link sign-in via auth-slice claim on `extensions.legacy_individual_id='c-001'`) + 3 gifts (Marcus's, fresh UUID ids, `recipient_org_id` NULL all 3 by design — none match catalog, `recipient_org_name` carries display). Determinism: all UUIDs pre-computed literals (not `randomblob()`), re-apply produces identical state. Normalizations applied: gift dates → ISO; Marcus budget → spaced `'$1K – $10K'`; org ids → `'org-N'`; Marcus causes → string-ID array. SQL-escape surface audited = exactly one site (org-12 mission `world's` → `'world''s'`); round-trip verified clean. Local verification PASS: counts 17/1/3, 3 gifts NULL-FK, 1 person NULL-auth, org-12 apostrophe round-trips to single quote, Marcus budget spaced + legacy id `'c-001'` retrievable, gift dates ISO (lexicographic = chronological sort proof). **NEXT**: FT-run cloud step (`wrangler login` → `d1 create stewardhouse-pilot` → real `database_id` swap → `migrations apply --remote` applies BOTH 0001+0002 to live DB → same 17/1/3 verification against remote), THEN auth wiring, THEN surface read/write. **PERSISTENCE LIVE ON REMOTE** (`84e66db` wired the real database_id). FT ran the cloud-step quartet this session: `wrangler login` → `wrangler d1 create stewardhouse-pilot` (real id `8600684c-25dd-4cfe-a41d-f3f6e53779c7`, region ENAM) → database_id swapped into `wrangler.toml` (`84e66db`, placeholder retired) → `wrangler d1 migrations apply --remote` rolled 0001 + 0002 to live D1 (17 + 4 commands, both ✅, matching local command counts exactly). Remote verification PASS, mirroring local: counts 17 orgs / 1 person / 3 gifts; 3 gifts NULL `recipient_org_id`; 1 person NULL `auth_user_id` (option B holds on remote — Marcus awaits first magic-link sign-in); org-12 mission apostrophe round-trips clean (`world's`); Marcus budget spaced `$1K – $10K` + legacy id `c-001` retrievable; gift dates ISO (lexicographic = chronological). All reads, zero rows written by verification. The schema-and-seed phase of the persistence arc is COMPLETE on remote — the app's first live backend. **NEXT**: auth slice (`better-auth` magic-link EMAIL-ONLY + D1 + CF Pages Functions per scoping-pass strand 2), which wires Marcus's `auth_user_id`-NULL row to its claim-on-first-sign-in path; THEN surface read/write wiring. **AUTH SLICE OPENED — sub-slice (a)** (`86ae99d`, local-smoke-verified then pushed). First FUNCTIONAL backend (request handler, not just schema). Migration 0003 (`21d746a`) added nullable `name`/`image` to `auth_user` (migration file shipped + local-applied at ship time; --remote apply was deferred and not run until 2026-06-30's slice-1 finalization, alongside 0004; remote schema was missing both columns from 2026-06-25 to 2026-06-30 — confirmed latent risk only, never triggered, since no remote sign-in occurred in that window — see slice-1 0003-gap audit) (better-auth 1.6.20 writes `name` on signup; 0001 had omitted both per ruling B — display identity still lives on `person.display_name`, `auth_user.name` is better-auth-internal write-through). Stack: `better-auth@1.6.20` + `kysely@0.29.2` + `kysely-d1@0.4.0` (all EXACT-pinned). `functions/api/auth/[[route]].js` = per-request `onRequest` handler (`makeAuth(context.env)` never module-hoisted — D1 binding changes per invocation); `functions/_lib/auth.js` = factory, raw Kysely over `D1Dialect(env.DB)`, snake_case modelName/fields maps matching 0001+0003, `session.cookieCache` disabled (#4203 defense), NO plugins / NO emailAndPassword / NO social. `wrangler.toml` gained `compatibility_flags=["nodejs_compat"]` + `pages_build_output_dir="dist"`. `.dev.vars.example` committed (placeholder; real `.dev.vars` gitignored). Local smoke (build-then-serve: `npm run build` && `npx wrangler pages dev`): `GET /api/auth/get-session` → HTTP 200, `application/json`, body `null` — six-axis green (function mounts, makeAuth constructs, D1 binds, snake_case resolves, nodejs_compat sufficient, cookieCache parses). CARRY-FORWARD to (b): (1) better-auth logs "Base URL is not set" under `pages dev` despite `.dev.vars` `BETTER_AUTH_URL` — env-loading quirk to resolve before magic-link URL construction; (2) prod `BETTER_AUTH_SECRET` must be set in CF Pages env-vars dashboard or `/api/auth/*` 500s on remote (expected until set); (3) local-dev: the `pages dev -- npm run dev` proxy form conflicts with `pages_build_output_dir` ("directory OR proxy, not both") — build-then-serve is the working path. Scoping-pass strand-2 caveats still live for (b): magic-link plugin + sender abstraction (CF Email BETA behind swappable interface, Resend GA primary at pilot), then (c) Marcus claim-on-first-sign-in hook (every auth_user gets a person; idempotent WHERE `auth_user_id IS NULL` + log re-claims; hardcoded `'c-001'`; no email-gate at pilot; post-commit after-create hook). **NEXT**: sub-slice (b) — magic-link plugin + Resend sender + resolve `BETTER_AUTH_URL` loading; FT provides `RESEND_API_KEY` + `FROM_EMAIL` (verified domain) + prod `BETTER_AUTH_SECRET`. **AUTH SUB-SLICE (b) — SHIPPED** (`0cf0642`, local-smoke-verified end-to-end, pushed; **first real authentication the app has performed**). `magicLink` plugin from `better-auth/plugins/magic-link` mounted on the factory: `expiresIn: 300`, `disableSignUp: false`, `storeToken: 'hashed'` (token stored as hash at rest, never plaintext), `rateLimit: {window:60,max:5}`; per-request `createSender(env)` runs INSIDE the `sendMagicLink` callback (never module-hoisted — mirrors the per-request rule on `makeAuth`). `functions/_lib/sender.js` (new) = `createSender(env)` factory keyed on `env.SENDER_PROVIDER`: `'resend'` = REST direct `POST https://api.resend.com/emails` with `Authorization: Bearer ${env.RESEND_API_KEY}` (no `resend` npm dep — Workers' global `fetch`); `'cf-email'` throws (deferred to sub-slice (d) when CF Email Service exits BETA); unknown provider throws. **Never logs the API key; never logs the success body** (which carries the Resend message id). Email copy in CLAUDE.md §7 quiet/editorial voice — subject `"Sign in to StewardHouse"`, plain-text + minimal `<p>`-only HTML, `&`-only escape on the visible URL (magic-link URLs carry raw `&` between query params), no exclamation, no "click here", "five minutes" spelled out, soft-out "If you did not request this, you can disregard this message". `.dev.vars.example` extended with three documented placeholders (`SENDER_PROVIDER`, `RESEND_API_KEY`, `FROM_EMAIL=onboarding@resend.dev` — the literal `onboarding@resend.dev` is non-secret, Resend's public test sender). **Smoke proven end-to-end on local** (`npm run build && npx wrangler pages dev`, `.dev.vars` auto-loaded per `wrangler-dist/cli.js:147199`): `POST /api/auth/sign-in/magic-link` → `{"status":true}`; magic-link email delivered to FT's Gmail via `onboarding@resend.dev`; link click → `GET /api/auth/magic-link/verify?token=…` → `auth_user` row created (id `wzWWG1X8YJHscHFxcOMWDCKA57Kx28oa`, `email='talabifaouzi@gmail.com'`, `email_verified=1`, `name=''`), 1 `session` row, 0 pending `verification` rows (token consumed atomically per GHSA-hc7v-rggr-4hvx). **The (a) "Base URL is not set" warning is resolved** — root cause was `.dev.vars` simply absent at the (a) smoke, not a wrangler quirk; with the file populated at repo root, wrangler auto-loads it and `env.BETTER_AUTH_URL` resolves correctly. **No new deps** (magic-link plugin ships inside better-auth via subpath; Resend uses native `fetch`). Commit delta 3 files / 116 insertions / 1 deletion. **Carry-forward**: prod needs all 5 vars set in the CF Pages env-vars dashboard (Production + Preview) before remote auth works; the new `auth_user.id` is NOT yet joined to Marcus's pre-seeded `person` row (`auth_user_id` still NULL on the seed person; the claim is sub-slice (c)'s job); pilot Resend sender restricted to account-owner email until a verified domain replaces `onboarding@resend.dev`. **PER-USER ENTRY + DEMO ISOLATION ARC — DESIGN PASS OPENED, NOT YET BUILT** (FT ruling this session). RULING: **single codebase, auth-gated modes** — NOT a forked pilot copy. Unauthenticated visitors get the existing 4-views investor/demo surface (seed fixtures, the entire current build). Authenticated pilot users land in an isolated per-type surface with their own activity persisted in D1. Demo and pilot are two doors into one app, gated by auth state + user type; no second codebase to maintain. **Three isolated landing pages**: Individual → Individual welcome flow; Enterprise → their org + athletes; Advisor → their practice + clients. **No escape hatch** for an authenticated pilot user — logo link, nav/menu, AND direct-URL access ALL keep them inside their own surface (no path back to the demo or another user-type surface; demo/seed data NEVER visible to a pilot user). **User TYPE** (`'individual'` | `'enterprise'` | `'advisor'`) stored on the `person` row and read by the router at sign-in to route to the correct surface. **OPEN QUESTION for the scoping pass**: how `type` gets ASSIGNED at invite time — pre-seed the `person` row before invite (FT controls assignment; `type` already on the row at first sign-in) vs encode `type` in the invite token itself (claim hook sets `type` from invite payload on first sign-in). **Session continuity**: tune `better-auth` session lifetime so pilot users aren't re-authing constantly (config decision, not a rebuild). Note: magic-link accounts ARE persistent — same email re-matches the same `auth_user` via better-auth's `findUserByEmail` path in verify; account + activity persist across sign-ins. **The friction to solve is session expiry, not account loss.** **Activity persistence (the wire-surfaces phase — the real payoff)**: surfaces must WRITE user activity / inputs / decisions to D1 keyed to the signed-in `person.id`, and READ them back on return. Surfaces are currently fixture-driven and persist nothing per-user; this is where the schema (`gift`, `scenario`) starts carrying real per-user data. **Named deliverable for the scoping pass**: MAP each of the three landing pages field-by-field — what each shows — confirming no demo data leaks in. **SEQUENCE**: (b) shipped → build (c) claim hook (the `auth_user` ↔ `person` join is the identity spine the entry arc + the wire-surfaces phase both hang off) → THEN scope + build the per-user entry/isolation arc on top. **NEXT**: sub-slice (c) — Marcus claim-on-first-sign-in hook via `databaseHooks.user.create.after`; idempotent `WHERE auth_user_id IS NULL` lookup keyed on `json_extract(extensions, '$.legacy_individual_id') = 'c-001'`; for new accounts not matching the seed, create a fresh `person` row in the same hook; log re-claims for visibility; no email-gate at pilot; post-commit after-create timing. **AUTH SUB-SLICE (c) — SHIPPED** (`66312bc`, claim path SQL-verified + fresh-person branch logic-verified local; pushed). `databaseHooks.user.create.after` on the `betterAuth` factory, closing over the per-request Kysely `db` from `makeAuth(env)`; slotted after `verification:`, before `plugins:`. **CLAIM path**: `UPDATE person SET auth_user_id = user.id WHERE auth_user_id IS NULL AND json_extract(extensions, '$.legacy_individual_id') = 'c-001'` — narrow seed-specific match for Marcus; comment marks it the **EXTENSION POINT** for future bespoke claims (do NOT broaden to arbitrary matching). **FRESH-PERSON path** (`claimed===0`): `INSERT INTO person` with fresh UUID + `auth_user_id` + `display_name='New user'` + `type='individual'` + `source_surface='individual'` — the 4 NOT-NULL person columns per migration 0001 (`id`, `display_name`, `type`, `source_surface`); `initials` / `extensions` / `soft_deleted_at` / `deletion_state` all nullable and omitted. **multi-match** (`claimed>1`): `console.warn` + treat as claimed (no fresh insert). Entire hook body wrapped in `try/catch` → log (**PII-FREE**: only `auth_user.id`, `person.id`, outcome string, claimed count; NEVER `email` or `name`) and **SWALLOW** so a hook error never blocks sign-in completion (the `auth_user` + `session` are already minted upstream). **No new deps** (`sql` is a named export of the already-installed `kysely@0.29.2`). **BESPOKE-TYPE RULING (FT, this session)**: organic self-signup is **ALWAYS `type='individual'`** — the only self-serve tier; sees only its own data, so the self-assigned type is safe. `'enterprise'` / `'advisor'` are **BESPOKE** — FT/staff build those accounts + logins together WITH the org / practice and **PRE-SEED the typed `person` row** before issuing the invite, so the bespoke user's first sign-in is a **CLAIM** of an existing typed row, never a fresh insert. The hook **NEVER** creates a privileged type. **Isolation guarantee for the entry arc**: picking a `type` yields that INTERFACE SHELL, but privileged surfaces show only data that the account owns or is assigned (empty-then-yours) — never demo data, never another account's roster / clients / participants. Isolation comes from **per-account data scoping in the wire-surfaces phase**, not from gating the type at signup. **VERIFICATION**: Marcus's one-time manual claim applied LOCAL (his `wzWWG1X8YJHscHFxcOMWDCKA57Kx28oa` sign-in pre-dates the hook; seed person `01000000-0000-4000-8000-000000000001` now linked to `wzWWG`, `display_name='Marcus Thompson'` + `type='individual'` preserved). Fresh-person branch logic-verified by running the hook's EXACT claim-UPDATE (0 rows — c-001 already claimed by Marcus → falls through to fresh-person branch) + fresh-person INSERT (valid `type='individual'` row landed) directly against local D1, then cleaned to baseline (1 `person` / 1 `auth_user` — Marcus only, baseline restored exactly). **CARRY-FORWARD**: (1) full end-to-end fresh-person sign-in via a REAL magic-link is **UNTESTED** until a verified **DOMAIN** replaces `onboarding@resend.dev` — Resend's test sender 403s on any non-account-owner email, so a second / new email can't receive a link (confirmed this session: `+alias` sign-in → Resend `403 validation_error`; send fails BEFORE token / user creation, so it **fails safe** — no orphan `auth_user` was left behind). The hook ↔ better-auth runtime wiring is the only unexercised gap; the SQL it emits is proven. (2) Marcus REMOTE claim — **RESOLVED-BY-DESIGN, NOT a manual write.** Investigation this session proved the manual `--remote` UPDATE CANNOT succeed: it sets `person.auth_user_id='wzWWG1X8YJHscHFxcOMWDCKA57Kx28oa'`, but that `auth_user` exists ONLY in local D1 (created by the (b) smoke's magic-link click, which ran against the local mirror via `wrangler pages dev` — never remote). On remote, the FK `person.auth_user_id REFERENCES auth_user(id)` has no such target → the UPDATE fails with `SQLITE_CONSTRAINT_FOREIGNKEY (7500)`. (A first attempt returned a transient `7403` that masked the real `7500`.) The CORRECT path: remote Marcus parity happens **automatically via the (c) hook on FT's FIRST real sign-in through the DEPLOYED remote app** — better-auth mints a fresh remote `auth_user` (new id, NOT `wzWWG`), `create.after` fires, matches Marcus's c-001 seed row (`auth_user_id IS NULL`), and claims it with a valid remote FK target. **NO manual UPDATE**; planting a fake `wzWWG` `auth_user` row to satisfy the FK was considered and **CORRECTLY DECLINED** (phantom user, no real auth provenance). Gated on: prod 5 env vars set in CF Pages dashboard + a deployed sign-in path (domain). Until FT signs in remote, Marcus's remote `person` row stays `auth_user_id IS NULL` — no functional issue, nothing depends on it. (3) **DOMAIN is now the gating real-world prerequisite** for BOTH full fresh-person testing AND sending magic-links to real pilot users who aren't FT. **NEXT**: (i) remote Marcus parity happens automatically via the (c) hook on FT's first deployed remote sign-in — NO manual `--remote` claim (see corrected carry-forward (2)); gated on prod 5 env vars in CF Pages dashboard + domain; (ii) THEN the per-user entry / demo-isolation arc scoping pass (three isolated landing pages mapped field-by-field; how staff pre-seed enterprise / advisor `person` rows + corresponding `auth_user` invites; session-lifetime tuning) and / or the **wire-surfaces phase** (surfaces write/read per-user data keyed to `person.id` — the real payoff); (iii) **DOMAIN acquisition** unblocks real pilot-user onboarding. **AUTH ARC PROVEN LIVE ON PRODUCTION — 2026-06-30 session** (four commits: `834c1ff` slice-1 keystone, `132be60` migration 0005, `1e0bade` R2 seed script, `ead8257` /signin + /app, plus `3a65e91` Landing visibility fix). Migration 0004 (`834c1ff`) added the `invite_email` column + UNIQUE index on `person(invite_email)`, retiring the c-001 identity-race bug in favor of email-match claiming — the (c) hook's WHERE clause pivoted from `json_extract(extensions,'$.legacy_individual_id')='c-001'` to `invite_email = user.email`, with the multi-match `if (claimed > 1)` warn branch reduced to a defensive trip-wire (structurally unreachable given the UNIQUE index; kept as post-hoc logging, not a pre-flight guard — an important nuance for future readers, since Kysely's `.executeTakeFirst()` on an UpdateQueryBuilder does NOT add `LIMIT 1` and SQLite's UPDATE has no LIMIT clause in D1). Migration 0005 (`132be60`) added 3 fictional demo-roster rows in the pending state (Jordan Avery `type='staff' source_surface='enterprise'`, Morgan Walker `type='advisor' source_surface='advisor'`, Reese Donovan `type='ops' source_surface='operations'`; all `demo-*@example.invalid` — RFC 2606 syntactically-valid never-deliverable TLD, so these rows can never be accidentally claimed by a real sign-in). Dedup note baked into the SQL comment: "Morgan Walker" appears in BOTH `enterpriseFixtures.contacts.morgan` (enterprise surface, role facilitator) AND `clients.advisorPracticeProfile` (advisor surface, practice lead) — one person, two fixture surfaces; the migration seeds ONE row (the advisor-surface version, the richer/canonical record) per CLAUDE.md §4's deferred-same-person-dedup pattern. R2 seed script (`1e0bade`) at `scripts/seed-invites.mjs` — reads a gitignored `scripts/pilot-invites.json` roster (`.example` counterpart committed as template, `scripts/pilot-invites.json` + `scripts/*.tmp.sql` added to `.gitignore`), validates each entry + normalizes emails (`.trim().toLowerCase()`) + pre-flight-rejects duplicates BEFORE any D1 write + generates a temp `.sql` batch + shells to `wrangler d1 execute --file=` + cleans up the temp file on BOTH success and failure paths. Smoke-proven: normalization (deliberately messy `"  SmokeTest1@Example.invalid  "` landed as `smoketest1@example.invalid`); pre-flight duplicate rejection (3rd entry `SMOKETEST1@Example.invalid` — different case + no whitespace — rejected with `[seed-invites] ERROR: entry #2 (SMOKETEST1@Example.invalid): duplicate email "smoketest1@example.invalid" within roster file`, exit 1, person count stayed +2 not +3, no temp SQL file was written on the rejected run). `/signin` + `/app` (`ead8257`) — magic-link sign-in form at `/signin` that POSTs `{email, callbackURL:'/app', errorCallbackURL:'/signin'}` (the `newUserCallbackURL` field was included initially then trimmed pre-commit as redundant per better-auth's documented fallback-to-callbackURL default), + `/app/*` post-auth session-check stub via `AppEntry.jsx` (GET `/api/auth/get-session` → `Navigate to="/signin" replace` if unauthenticated OR render the "You're signed in" panel with `session.user.email` + a sign-out button that POSTs `/api/auth/sign-out`). Landing.jsx acquired an "Already invited? Sign in" link — first placed after the disclaimer at slice ship time (`ead8257`), then relocated (`3a65e91`) per FT's live-site usability read to a prominent sibling between the hero subtitle and the surface-picker grid, styled body-text register with bronze underlined "Sign in" link. **FULL LOOP END-TO-END PROVEN ON THE LIVE DEPLOYED SITE, not just local**: FT completed a real magic-link click-through against the deployed CF Pages app + production D1, session cookie set, /app rendered authenticated, sign-out worked. **PRODUCTION SECRETS GAP CLOSED — root-cause + two sharp edges (essential future-debugging captures)**. All 5 prod env vars (`BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `RESEND_API_KEY`, `SENDER_PROVIDER`, `FROM_EMAIL`) had been a carry-forward since the very first (a) auth session — but were NEVER ACTUALLY SET on Cloudflare Pages production despite the carry-forward text saying they'd need to be. Discovered by a real live sign-in test failing with "Sign-in is temporarily unavailable." Root-caused via `wrangler pages deployment tail` — **this is the essential first-resort debugging tool for any "works locally, fails live" auth problem going forward, NOT last resort**. Two additional sharp edges surfaced during the fix: **(a)** `RESEND_API_KEY` was accidentally set from the committed `.dev.vars.example` placeholder template value instead of the real `.dev.vars` — a warning comment inside `.dev.vars.example` would prevent recurrence, worth adding in a future doc pass. **(b)** A stuck PowerShell paste prompt silently captured a subsequent typed command as the literal value of `SENDER_PROVIDER` — `wrangler pages secret put` interactive prompts are a real footgun; **prefer piping values directly (`"value" | npx wrangler pages secret put KEY --project-name=...`) over interactive paste going forward.** **CF Pages secrets only bind at DEPLOY time, not live** — any secret change requires a fresh `wrangler pages deploy` to take effect. Cost real debugging time before being understood; recording so a future session doesn't burn the same cycles. **MARCUS IDENTITY RECONCILED ON REMOTE — schema-change-vs-seed-data gap (recurrence-preventable lesson)**. First real production sign-in exposed a real design gap that slice-1 (`834c1ff`) introduced without noticing: the email-match schema change left Marcus's original 0002-seed person row with `invite_email = NULL` (predates migration 0004, no backfill), so the (c) hook's claim branch found ZERO matches on FT's first live remote sign-in and fell through to the fresh-person branch, creating an orphaned "New user" row (id `27027bf5-dbcc-4a97-82e6-5ffd699e3db2`) bound to FT's `auth_user` (`w6Y38Nn3Se5uNlYJHszJLC4HIs14Zc5h`) instead of claiming Marcus's seed row (`01000000-0000-4000-8000-000000000001`) — **silently breaking schema ruling F's founding intent ("Marcus = FT's first real account through real auth")**. Reconciled via UPDATE-in-place, **NOT delete-and-reinsert** (which would have cascade-deleted Marcus's 3 real gift records via `gift.giver_person_id ON DELETE CASCADE`): (1) `UPDATE person SET invite_email='talabifaouzi@gmail.com' WHERE id='01000000-...'` — schema-correct fix, not one-shot workaround; if FT's session ever needs to re-derive identity or if any future sign-in retry happens, the row is now properly matchable under the current claim key. (2) `UPDATE person SET auth_user_id='w6Y38Nn3...', display_name='Faouzi Talabi' WHERE id='01000000-...'`. (3) Gate: verified `count(*) FROM gift WHERE giver_person_id='27027bf5-...'` = 0 BEFORE deletion; then `DELETE FROM person WHERE id='27027bf5-...'` — cascade fired on 0 rows as expected. Final remote state verified: FT's account carries Marcus's 3 seed gifts ($5,000 Cleveland Youth Hoops Foundation / $2,500 Northeast Ohio Sports Access Coalition / $1,000 Cleveland Public Schools Athletics Fund) as real giving history; `person_ct=4` (Marcus/FT reconciled + 3 demo roster); `auth_user_ct=1` — exactly as ruling F intended. **LESSON — recurrence prevention**: any future schema change to a claim-matching mechanism must audit whether PRE-EXISTING seed rows need backfilling under the new match key; this specific gap could recur for any other pre-0004 seed data that might need to be claimable. **NEXT SESSION OPENS WITH — Individual-wiring slice**. Wire `/app`'s current stub into a real type-dispatcher (reads `person.type` for the signed-in user, routes to `/app/individual/*` for `type='individual'`, `/app/enterprise/*` for `type='staff'`, `/app/advisor/*` for `type='advisor'`, `/app/operations/*` for `type='ops'`) + build the `/app/individual` authenticated empty shell as the first live-wired surface. **Investigation already found this session's tail (read-only)**: **(a)** the data contract is nearly free — `IntakeContext.jsx:79-103`'s existing `resetIntake()` method already produces the exact correct empty shape for all 4 zero-data-audit fields (`answers.causes: []`, `gifts: []`, `lessonsDone: []`, `assignmentsDone: []`; the last two are already the DEFAULT_STATE default, only the first two need swapping). **(b)** The real complexity is routing — `IndividualSurface.jsx` carries ~10-15 hardcoded `/individual/...` paths (the 6-item `NAV_ITEMS` const at :30-37, 8 `navigate('/individual/...')` calls inside `IndividualHome`, 1 `Navigate to="/individual"` fallback in `DashboardLayout` at :92) that would silently misroute an authenticated user back to the public demo if the component were mounted as-is under `/app/individual/*`. Four candidate approaches identified with structural trade-offs (separate wrapper component / prop-based basePath swap / dual-mount at App.jsx level / runtime location detection); all share the common denominator that the hardcoded paths must be touched. **(c)** A new `functions/api/me.js` (or similarly-named) endpoint is needed for the type-dispatcher — calls `makeAuth(env).api.getSession({ headers: request.headers })` + a Kysely `SELECT type, display_name FROM person WHERE auth_user_id = user.id` join, **the first consumer of better-auth's direct server-side `.api.*` interface in this codebase** (pattern confirmed against `node_modules/better-auth/dist/auth/base.mjs:35` — `betterAuth(options)` returns `{ handler, api, options, $context, $ERROR_CODES }` and `api` exposes every endpoint as a directly-callable function bypassing HTTP; no HTTP round-trip needed within the Worker). **(d)** STILL OPEN before build starts: confirm where `IntakeProvider` is mounted (likely `main.jsx`, not yet read) to determine whether public-demo (`/individual/*`) and authenticated (`/app/individual/*`) state can safely coexist under a single provider instance or whether the authenticated mount needs its own provider tree to prevent state leaking between the two surfaces. **INDIVIDUAL-WIRING ARC COMPLETE — 2026-07-01 session** (multi-slice arc across a single session, culminating in the authenticated surface actually working end-to-end with correct data + path isolation). Rich commit chain: `94cd683` piece 1+2 IntakeProvider two-instance split; `6712f8a` piece 3 /app dispatcher + AppShell + AppDispatcher + AppIdentityContext + /api/me + IndividualSurface basePath refactor; `910abc2` Landing pilot-access converted to secondary Button; `ab13bab` §6.11 rule bank (authenticated-surface path audit — see below); `a93b2d8` piece 3.b useBasePath extraction + 8-file navigate() migration; `07447bd` three fixes (demo-restore hidden on authenticated tree / Athletics filler removed / intakeComplete gate added); `a463069` Fix D+E (onboarding-restart hidden post-completion / Questions.jsx resume-at-first-unanswered). CF Pages auto-deploy picks each push up on the `stewardhouse-app` project (correct name — NOT `stewardhouse-pilot`, which is the Worker/D1 project name; recorded to prevent recurrence of the manual-deploy attempt that failed with `code 8000007 Project not found` this session). **(1) /app has a real type-dispatcher.** `functions/api/me.js` is the **first Kysely SELECT** and **first direct `auth.api.getSession()` consumer** in the codebase. Signature: `GET /api/me → 200 null` (no session) OR `200 {user: {email}, person: {type, displayName} | null}` (session present). Direct-call pattern documented via `node_modules/better-auth/dist/api/to-auth-endpoints.mjs:20` (explicit "Pass `headers: request.headers` (or `request`) to the call" guidance) — proven working end-to-end via curl's `null` round-trip (200 shape confirmed; `Content-Length: 4` = exact literal 4-char `null` body). `AppShell` fetches identity ONCE on mount and shares it via `AppIdentityContext` + react-router-dom `<Outlet>` to every child route — critically, this avoids a double-fetch across the `AppDispatcher → /app/individual` redirect boundary (dispatcher reads from context, doesn't re-fetch). `AppDispatcher` routes `person.type === 'individual'` to `/app/individual`; other types (`staff`/`advisor`/`ops`) get an **honest "Your {type} surface isn't built yet" placeholder** (not a silent failure or dead-end); a session with no matching person row gets a **defensive "We couldn't find your account" placeholder** (should be structurally unreachable given the (c) hook always claims or creates, but named-not-assumed). **(2) Data + path isolation on both trees.** IndividualSurface now renders correctly under BOTH `/individual/*` (public demo, seeded with Marcus's `DEFAULT_STATE`) AND `/app/individual/*` (authenticated, seeded with `AUTHENTICATED_EMPTY_STATE`). **Data isolation** (piece 1+2, `94cd683`): moved `<IntakeProvider>` from a single `main.jsx`-level provider to **two per-route mounts in App.jsx**, each with its own `useState` — demo drift and authenticated activity now CANNOT leak into each other. `AUTHENTICATED_EMPTY_STATE` const exported from `IntakeContext.jsx` (previously `DEFAULT_STATE` was module-private); `IntakeProvider` signature widened to `({ children, initialState = DEFAULT_STATE })`. **Path isolation** (piece 3 + 3.b, `6712f8a` + `a93b2d8`): shared `src/surfaces/individual/useBasePath.js` hook (`useLocation`-derived: `pathname.startsWith('/app/individual') ? '/app/individual' : '/individual'`) closes a path-leak bug found via **FULL-DIRECTORY audit** (not just the entrypoint file). The audit found 8 sub-screen files carrying **13 hardcoded `navigate('/individual/...')` call sites** — Positioning, Letter, Privacy, Questions, GPSReveal (the 5 onboarding screens), plus Plan, GiveScreen, Feedback. Every one would have silently ejected an authenticated user into the public demo tree — a **data-identity leak, not just a broken link**, because the demo tree mounts its own `<IntakeProvider>` seeded with Marcus's fixture, so the authenticated user's own view would be REPLACED by Marcus's data mid-session. Files confirmed clean (audited but no hits): History, Learn, Team, GivingModeler, Discover, CohortView. Standing lesson: **CLAUDE.md §6.11 rule** banked as `ab13bab` (Authenticated-surface path audit — full-directory rule) — any future authenticated-reuse slice for Enterprise/Advisor/Operations must run the same full-directory grep as a required step, and should extract a shared basePath-derivation helper once 2+ files need it (per the standard #47/#57 threshold) rather than duplicating the logic per file. **(3) Identity display via `AppIdentityContext`.** Chrome now shows the authenticated user's real `person.display_name` (fetched by AppShell, propagated via `AppIdentityContext`) instead of the hardcoded Marcus fixture. Uses a safe `useOptionalAppIdentity()` wrapper — a try/catch around `useContext(AppIdentityContext)` — so the **same IndividualSurface component tree works on both the context-free public demo AND the context-bearing authenticated tree without crashing**. Rules-of-Hooks note: `useContext` is a bare read (no `useState`/`useEffect` slot), so the try/catch pattern is safe by construction; documented in an inline comment at the wrapper site. If `useAppIdentity` ever gains its own stateful hooks, the wrapper pattern breaks and needs revisiting — the eslint-disable-next-line is prophylactic against a future ESLint pass, not a suppressed live warning. Chrome consumer at `IndividualSurface.jsx` DashboardLayout: `userName={appIdentity?.status === 'ready' && appIdentity.identity?.displayName ? appIdentity.identity.displayName : individualProfile.name}` (falls back to Marcus fixture on public demo); `userRole={appIdentity?.status === 'ready' ? 'Member' : `Member · ${individualProfile.worldLabel}`}` (`'Member'` chosen over prompt-suggested `'Individual pilot'` — voice/tone: "pilot" is internal StewardHouse language, `'Member'` preserves the existing "Member · Athletics" register just without the sector suffix, and echoes Chrome's `SURFACE_CONFIG.individual.role: 'Member view'` on the left cluster). **(4) Pilot-driven UX fixes** (found via FT's own first real use of the authenticated app — exactly what a pilot is for): **(a)** "Restore Marcus's demo profile" now hidden on the authenticated tree via `{gifts.length === 0 && !appIdentity && (...)}` guard (`07447bd`). Prior guard `{gifts.length === 0 && (...)}` was tree-blind — a fresh authenticated user's empty gift count matched the guard, so the button was rendering on the authenticated tree. Clicking it would call `loadDemo()` → `setState(DEFAULT_STATE)` → the authenticated user's IntakeContext would be REPLACED with Marcus's full fixture. **Real data-corruption risk**, not just cosmetic. **(b)** Removed the hardcoded `{worldLabel}` filler `<p>` from the Identity anchor Card (`07447bd`) — Phase 1 is athletes-only per Tier 1 sweep, telling an athlete they're an athlete is pure noise; the `<div>` rendering `{givingStyle || 'Intentional Giver'}` stays (real derived signal). Layout-orphan observation flagged but not restructured: the removed `<p>` had `marginBottom: 'var(--sh-space-2)'` that spaced it from the pill below; post-removal the pill sits at Card's default top padding, slightly airier than before but not a broken layout — FT can decide if it wants a rhythm adjustment. **(c)** Added an onboarding-completion gate on DashboardLayout (`07447bd`): `const { intakeComplete } = useIntake(); if (!intakeComplete) return <Navigate to={\`${basePath}/welcome\`} replace />;` — a fresh authenticated user (with `AUTHENTICATED_EMPTY_STATE.intakeComplete: false`) is redirected to the onboarding flow (welcome → letter → privacy → questions → reveal) before ever seeing Home, instead of landing on a dashboard that assumed a completed giving plan (Home renders 6 PathCards + pulse stats + identity anchor — all of which read from `answers` that wouldn't have been populated yet). **Verified NOT assumed**: `DEFAULT_STATE.intakeComplete = true` (re-read at IntakeContext.jsx:13), so the gate cannot fire on the public demo tree; only fires on the authenticated tree with fresh empty state. **(d)** Hid the "See the new-user onboarding flow →" restart button once intake is complete (`a463069`) via `{!intakeComplete && (...)}` wrapper. **Confirmed via render-order trace to be fully dead code post-gate** — DashboardLayout's gate returns Navigate before IndividualHome can mount, so IndividualHome only ever renders with `intakeComplete === true`. Kept the conditional anyway as cheap defensive self-documentation — if FT ever weakens or removes the gate, this button won't reappear incorrectly. **(e)** Questions.jsx now resumes at the first unanswered step (`a463069`). Prior `useState(0)` was tree-blind — forced click-through of all 12 steps even when answers were fully populated in IntakeContext. New lazy initializer: `useState(() => { const firstInvalid = steps.findIndex((s) => !s.valid); return firstInvalid === -1 ? steps.length - 1 : firstInvalid; })`. Uses each step's own `.valid` field (authoritative "is this answered" check — encodes compound rules like Q3's `a.geo.length > 0 && (a.geo.every(id => !['hometown','current','state'].includes(id)) || (a.geoDetail || '').trim().length > 0)`). Adaptation note: the prompt's structural sketch used `steps.findIndex((s) => { const val = a[s.key]; ... })` but `.key` doesn't exist on step objects; adapted to `!s.valid` which is strictly better (respects compound validity rules without duplicating them). Rules of Hooks: `useState(step)` and `useState(showBreak)` were moved from BEFORE the `steps` array construction to just AFTER it, keeping the RELATIVE order between the two useState hooks preserved (`useNavigate → useBasePath → useIntake → useState(step) → useState(showBreak)`). Both fixes **close the "have to redo everything" perception bug from browser back-navigation**. Read-only trace confirmed this bug was NOT actually destructive at the data layer: `updateAnswer` / `toggleAnswer` use spread-preserve patterns that never touch `intakeComplete`; browser navigation doesn't remount the provider; `completeIntake` is the ONLY code path that writes `intakeComplete: true`, called only from Questions.jsx's last-step `next()` closure. The symptom was forced click-through, not data loss. **(5) Production secrets + Marcus reconciliation FULLY CLOSED** (carried from the prior session's bank, now completed). All 5 prod CF Pages env vars (`BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `RESEND_API_KEY`, `SENDER_PROVIDER`, `FROM_EMAIL`) confirmed set and verified working via a **live end-to-end sign-in against the deployed CF Pages app** — magic-link click-through completed, session cookie set, `/app` rendered authenticated, sign-out worked. Marcus's original 0002-seed `person` row (with `invite_email` NULL — pre-dating migration 0004 slice-1) is now reconciled to FT's real `auth_user`: backfilled `invite_email = 'talabifaouzi@gmail.com'` (schema-correct forward fix, not a one-shot manual write); rebound `auth_user_id` to FT's `w6Y38Nn3...`; renamed `display_name` to 'Faouzi Talabi'; the orphaned "New user" fresh-person row (`27027bf5-...`, created when the (c) hook fell through to the fresh-person branch because the initial live claim found zero email matches) was deleted after gate-verifying 0 gifts on it. **Marcus's 3 original seed gifts ($5,000 Cleveland Youth Hoops / $2,500 Northeast Ohio Sports Access / $1,000 Cleveland Public Schools Athletics Fund) preserved as FT's real giving history** — schema ruling F's founding intent held. Final remote state: `person_ct=4` (Marcus/FT reconciled + 3 pending demo roster), `auth_user_ct=1`. **(6) KNOWN FOLLOW-UP, NOT YET BUILT — geo-selection weighting.** Read-only investigation confirmed the 5 `GEO_OPTIONS` pills (`hometown`/`current`/`state`/`national`/`international`) have **NO real weighting or conflict resolution today**. Selecting contradictory pills (e.g. `hometown` + `international`) produces INDEPENDENT ADDITIVE effects, not a resolved preference. **3 of 5 pills are effectively display-only** (`current`, `state`, `national` don't feed Discover's scoring or `deriveGivingStyle` at all — they only gate whether the `geoDetail` text input appears and get joined into GPSReveal's raw-ID fallback text). The actual local-matching signal driving Discover's scoring is the freeform `geoDetail` text field (matched via `org.geo.toLowerCase().includes(activeGeo.split(',')[0].toLowerCase().trim())` at +2), NOT the pills themselves. `'international'` grants +2 on International-tagged orgs; `'hometown'` triggers 3 giving-style branches (`Hometown Starter`, `Community Anchor`, `Rooted Giver`) and 2 celebration branches; the other 4 pills have zero direct effect on either. Also discovered: **GPSReveal raw-ID fallback display bug** at `GPSReveal.jsx:28` — `const geoText = a.geoDetail || (a.geo || []).join(', ');` shows literal IDs like `'hometown, international'` instead of human labels (`'My Hometown, International'`) when `geoDetail` is empty. Needs a real design pass — **what should each pill actually mean?** — before any code change. Deliberately NOT built this session, parked as a named follow-up per FT's explicit "not as important, continue with predetermined order" ruling. **(7) NEXT — Operations roster UI** (both demo and live-gated modes per Ruling 1.1), then the invite-creation form on top of it (per FT's "do this through Operations" ask — folded to AFTER the roster read-side exists, per FT's own sequencing call). Enterprise/Advisor authenticated-surface hardening remains a later, separate arc, and will trigger the §6.11 full-directory audit rule established this session. The Individual authenticated surface is now the **first surface with real per-user data isolation, real identity display, and a real gated onboarding flow** — the reference pattern the other three surfaces will follow when their turn comes. **CONTINUING 2026-07-01 SESSION — post-wiring persistence arc + Advisor investigation opener.** (1) **GIFT WRITES + ENHANCEMENTS**: `POST /api/gifts` shipped (`8bcc00c`) — server-authoritative write flow where the client only updates local state AFTER server 200 confirms. Later enhanced (`192d640`) with currency formatting on the amount input, a purpose free-text field, a recurring-years int field (migration 0006), and a **5-option philanthropic gift-type taxonomy** — unrestricted / program / capital / capacity-building / endowment — replacing the old unrestricted-vs-directed 2-option toggle to match how funders actually think about gift categories. (2) **NAVIGATION / SESSION FIX CLUSTER** — three related fixes shipped as FT hit real friction living in the authenticated app: **(a) onboarding history-collapse** — all 10 restart-onboarding-style `navigate()` calls across 7 files gained `{ replace: true }` so browser-back after completing onboarding exits the app cleanly instead of walking back through 5 completed steps; **(b) /signin session-check** — checks for an existing session on mount and redirects straight to `/app` if already authenticated, closing the "back button makes me feel signed out" confusion; **(c) Landing.jsx session-check** — same pattern, swapping "Already invited? Sign in" for "Continue to your account" for already-authenticated visitors, with a calculated `minHeight` to prevent layout shift during the check. (3) **INTAKE PERSISTENCE (slices A + B)** — the real fix for "refresh sends me back to onboarding." **Slice A**: `POST /api/intake` write endpoint using SQLite `json_set` (NOT read-modify-write, to avoid a lost-update race — proven via local smoke test that plain-bound arrays silently double-encode as escaped strings unless wrapped in `json()`, and booleans need `json('true')` not a plain string); `/api/me` extended to return `person.extensions.individual`; `AuthenticatedIntakeProvider` bridges server data into `IntakeProvider`'s initial state. **Slice B**: `Questions.jsx` wired to the endpoint — BLOCKING full-answers-object write on every step-advance (chosen over per-field diffing for self-healing redundancy against dropped debounce writes — payload cost is trivially small, "least prone to data loss" was the explicit design standard); debounced 250ms per-field safety-net writes for the 5 per-keystroke text fields (`geoDetail` / `lived` / `influence` / `existingOrgs` / `legacy`), matching the codebase's existing debounce precedent (5 URL-state-sync consumers, same 250ms constant). (4) **GIFT READS** — `GET /api/gifts` + `/api/me` extended to include `person.gifts`, both sharing an identical 11-field response shape with the write endpoint (added `exportedToCpa: false` to the POST response for shape parity). AppShell / AppIdentityContext / AuthenticatedIntakeProvider widened a second time (gifts alongside intake). (5) **MARCUS UN-RECONCILIATION — production data correction.** FT's real account had inherited Marcus's 3 seed gifts via the original reconciliation (schema ruling F's original intent — Marcus's demo gifts becoming FT's real history) but this read wrong once FT was actually living in the account day-to-day. Un-reconciled via UPDATE-in-place (NOT delete-and-reinsert): Marcus restored as a fresh standalone person row (new UUID, full original migration-0002 seed shape, unclaimed) with his 3 seed gifts re-parented to him; FT's row reset to a genuine clean slate (extensions cleared, 0 gifts); a $1M GiveScreen smoke-test artifact gift deleted. FT's `auth_user_id` / `invite_email` were never touched, so no duplicate-account risk on next sign-in. (6) **SCENARIO PERSISTENCE** — `POST/GET /api/scenarios` shipped (`4e17a95`) snapshot-not-history per 5.8 doc — each save creates a new row, never updates. `GivingModeler.jsx` gained a save-scenario UI gated on `useOptionalAppIdentity` — the throw-free variant extracted from `IndividualSurface.jsx`'s local implementation into a shared `AppIdentityContext` export (genuine second-consumer threshold crossed per #47/#57; verified behavior-preserving by tracing `useContext`'s actual null-default semantics against the file's `createContext(null)` at the top, rather than assuming). Later extended (`4cdf278`) with `DELETE /api/scenarios/:id` — **first `[id].js` dynamic-route file in `functions/api/`**; scoped by id AND `owner_person_id` together so ownership can never be guessed around; 404 for both "doesn't exist" and "not yours" to avoid leaking existence — plus `removeScenario` context action and a "Manage saved scenarios" list with per-row delete controls (kept separate from the load `<select>`, since HTML forbids interactive controls inside `<option>` elements). (7) **GIVINGMODELER UX PASS** (multiple rounds, all FT-driven from actually using the product): typeable slider inputs (paired text field alongside every drag slider, synced both ways); flat-amount vs. percent-of-income giving mode (SegmentedControl-based toggle; percentage mode computes annual from `income × percentage` locally, never sent to the server as a separate concept — `annual` stays the only value that persists); load-a-saved-scenario picker (native `<select>`, matching the only existing dropdown precedent in the codebase, `DocCreate.jsx`); comma-formatted number display + decimal support for percentage fields with a float-precision fix — `Math.round(n * 10) / 10` for step<1 fields, avoiding JS's `5.300000000000001` artifact; larger/darker input text for legibility. **Bar chart fully redesigned for visual quality** — FT's flag: "tacky, not high-end" — real bronze-on-white contrast replacing the old 70%-opacity-on-near-matching-background, per-bar value labels (always visible on first/last bar, on hover for others, with real `aria-label`s), a visible baseline via bottom border, presence (110px → 156px height across iterations). Required **TWO follow-up geometry fixes**, both derived from real CSS-mechanics arithmetic (not assumed): **(a)** peak-bar labels were overflowing the chart's own top edge — fixed by growing top padding / height with the overflow math re-run against the new values to confirm a genuine negative margin, not just a smaller positive one; **(b)** hovering the 2nd or 2nd-to-last bar collided its label with the always-visible edge label next to it — fixed by lifting just those two positions' hover label higher, which re-triggered the top-overflow fix's math and required growing the container further (final: `height: 156px`, `paddingTop: var(--sh-space-10)` = 40px). Both fixes re-verified with full arithmetic before shipping — not shipped on the first attempt that "should" have worked. (8) **GIFTROW RULING** — investigated extract-vs-migrate-in-place per the 5.8 doc's open question; found the doc's stated justification for EXTRACT ("about to gain a real second consumer") didn't hold — `History.jsx` only computes gift aggregates, never renders per-gift rows. **RULED: migrate-in-place, not extract**, per the #47/#57 no-premature-abstraction lens this project has held consistently. GiftRow stays a private `IndividualSurface.jsx` subcomponent. (9) **PARKED FOLLOW-UPS** (not built, named for later): **account-settings page** (clicking user initials / profile — routing + scope undecided, needs its own scoping pass); **standing mobile-rendering constraint** — some pilot users are on phones; verification for visual builds should include explicit CSS-mechanics reasoning, not just route-resolves checks, since Claude has no visual rendering access this session. (10) **NEXT — Advisor persistence arc OPENED, investigation-only, no build yet.** Confirmed: `AppDispatcher` already has an honest advisor-type placeholder ready for a real branch; `AdvisorSurface` is at Individual's PRE-persistence baseline (zero D1 reads, two fixture-seeded context providers — `PracticeContentProvider` + `DocumentationProvider` — instead of Individual's one); §6.11 path audit found **46 hardcoded `/advisor/` path sites across 13 files** (vs Individual's 13 sites / 8 files — roughly 3.5× larger surface); fixture inventory captured (`data/clients.js` as the dominant fixture at 880 lines, plus `content.js` / `practiceContent.js` / `documentation.js` / `cohorts.js`) for a LATER persistence design pass. **Sequencing plan**: small routing-only slice first (dispatcher branch + `/app/advisor` route, NO data isolation yet — deliberately incomplete and flagged as such), same shape as Individual's very first piece-3 slice, before committing to the larger 46-site audit or persistence design in one push. |
| **Landing** | Single file; public entry. |

---

## 6. Slice protocol (build discipline)

Every substantive change runs as a **slice**. The rhythm:

1. **Hard git-state gate.** Confirm current branch and HEAD before starting.
   For most slices: `main` at the expected SHA, working tree clean. Refuse to
   proceed if state is unexpected.
2. **Audit + propose.** Investigate read-only; propose a spec with explicit
   scope, decisions, verification plan, and suggested commit message. Hold for
   review. **Do not edit until decisions are locked.**
3. **Feature branch off main.** One slice = one branch (`slice-{letter}-{name}`,
   `fix-{topic}-bundle-{n}`, `docs-{topic}-refresh`, etc.).
4. **Write the slice.** One file at a time when possible; no truncation; no
   "rest unchanged" markers. Flag anything unusual; do not fix unrelated issues
   inline.
5. **Verify.** `npm run build` clean; node smoke against `unified.*` where
   relevant; per-pattern counts for data slices; UI slices add a dev-server
   visual check at the named URL.
6. **HOLD.** Report diffs, verification output, and visual-check URL when
   relevant. Wait for explicit go-ahead.
7. **Commit.** Exact message dictated by the founder. **Never append
   `Co-Authored-By` or any footer.** Never `--amend` after a hook failure (the
   prior commit didn't happen; fix and create a NEW commit).
8. **Bank.** `git checkout main; git merge --ff-only {branch}; git push origin
   main; git branch -d {branch}`. Fast-forward only — refuse if anything
   intervened on main.
9. **Audit branches are never merged or deleted.** `qa-audit-enterprise` and
   `qa-audit-operations` are reference-only and remain on the local clone +
   origin indefinitely. Findings flow into fix-bundle slices that branch off
   `main`, not off the audit branch.
10. **Migration discipline (local-then-remote rule).** Any commit that ships a
    `migrations/NNNN_*.sql` file must either (a) include the corresponding
    `wrangler d1 migrations apply --remote` step in the same ship operation,
    OR (b) explicitly carry forward a named "local-only, remote-apply DEFERRED
    to [specific gate]" note in CLAUDE.md so the deferral is a documented
    decision, never a silent gap. Discovered 2026-06-30 when migration 0003
    (`21d746a`, shipped 2026-06-25 local-only) was found unapplied on remote
    5 days later — latent risk only, never triggered (no remote sign-in had
    occurred in the gap window), but the same gap could have hard-failed
    real magic-link signup against a remote `auth_user` table missing the
    `name` column. See the AUTH sub-slice (a) bank's 0003-gap audit
    annotation in §5 (Individual row).
11. **Authenticated-surface path audit (full-directory rule).** When
    wiring any existing public demo surface for reuse at an authenticated
    route (e.g. IndividualSurface at both `/individual/*` and
    `/app/individual/*`), the hardcoded-absolute-path audit must cover
    EVERY file inside that surface's directory (e.g. all of
    `src/surfaces/individual/*.jsx`), not just the top-level surface
    file. Discovered 2026-07-01 during the Individual-wiring slice: piece
    3's basePath refactor correctly fixed IndividualSurface.jsx's ~16
    hardcoded paths, but a follow-up full-directory audit found 8
    sub-screen files — Positioning, Letter, Privacy, Questions, GPSReveal
    (the 5 onboarding screens), plus Plan, GiveScreen, and Feedback —
    carrying 13 hardcoded `navigate('/individual/...')` call sites,
    unaudited in that pass. (Learn, History, Team, GivingModeler,
    Discover, and CohortView were confirmed clean.) The
    failure mode: a signed-in pilot user clicking a "back" or navigation
    action inside a sub-screen gets silently dropped into the PUBLIC demo
    route — which mounts a SEPARATE IntakeProvider instance seeded with
    Marcus's fixture data, so the authenticated user's own view is
    replaced by the demo profile. This is a data-identity leak, not just
    a broken link. Any future slice wiring Enterprise, Advisor, or
    Operations for authenticated reuse must run the full-directory grep
    (every file in that surface's directory, not just the entrypoint) as
    a required step before considering the wiring complete, and should
    extract a shared basePath-derivation helper once 2+ files need it
    (per the standard #47/#57 threshold) rather than duplicating the
    logic per file.

Stop background shells (dev server, watch loops) at bank time. Use `TaskStop`,
not `kill`.

---

## 7. Guardrails

### Path B — the structural-not-advisory boundary

The platform organizes what advisors and funders decide; it does not decide
for them. **No AI-drafted Giving Partnership Profiles. No custody or payment
rails. No evaluative recommendations.** Test: exposure (in) vs. evaluative
recommendation (out). "You should fund X" or "Marcus is ready for a larger
gift" violates Path B. Surface options; never prescribe.

Not a robo-advisor for charity, not a competitor to advisor-client
relationships, not a fiduciary, not a fund custodian, not advisor-driven
matching with scores or rankings.

### No scoring, no ranking

Nonprofit profiles, advisor discovery, GPS, pipeline — narrative-based
throughout. **Issue severity (`low`/`normal`/`high`) rates the ISSUE's
triage urgency, never a participant.** The same applies anywhere a number
or letter could be read as a judgment of a person or org.

### Brand tokens only

Warm beige bg `#FAF7F2`, bronze accent `#8B7355`, white card surfaces,
Libre Baskerville serif headings, Inter/sans body. Every color / spacing /
radius / type-size goes through a `--sh-*` token in `src/styles/tokens.css`.
**No hex literals in components.** SVG icons only (`role="button"` divs get
the bronze focus-visible ring via global selector). **Zero emoji anywhere
in the interface.** WCAG AA.

### Voice & tone (LOCKED)

Quiet, editorial. Closer to Aesop or The Atlantic than a consumer app.
- No exclamation points except in dialogue.
- No "congratulations" or celebratory language for routine task completion.
- Sentences carry weight; clauses do work.
- Meet people where they are — never imply users are at a destination.
- Spelling: standard English (Merriam-Webster). Correct silently unless the
  meaning changes.

### Aggregate-default with purposeful drill

Default landing data is aggregate (counts, sums, rollups). Record-level
queries are exposed but explicit — `connectionsByGiver()` / `openIssues()`
are separate from `connectionFunnel()` / `openIssueCount()`. UI cards drill
to record level when there's a real target; when no target exists, render a
"detail view coming soon" footnote rather than a dead click.

### Names verbatim from records

Any participant or org name in user-facing copy must come from the assembled
record's `.name` field. Never fabricate, never paraphrase. The Slice C
verify pattern asserts that issue summaries contain the linked record's name
verbatim; the same discipline applies anywhere copy references a record.

### Demonstrative vs LIVE honesty boundary

Synthetic-derived UI must be labeled demonstrative. Only genuinely live
signals (e.g., `platformHealth()` traversing actual `runChecks`) may carry
LIVE framing. On the Operations Overview, the demonstrative-state caveat
explicitly excepts Platform health; all other regions read as synthetic
seed, not live platform traction.

### Section 6 vocabulary (LOCKED)

"Between-session pipeline" (the name), "surface" (the unified verb), states
are Active / Mute / Pause, "cohort updates" is the fifth content type.
Per-client settings are "default" vs "override"; overrides are preserved
when practice-wide defaults change.

### Section 6 data reconciliation invariant

The advisor roster has **9 clients** (c-001 .. c-009). All Section 6 numbers
must reconcile:
- Each client carries a `pipeline` array: one entry per content type, with
  `state` (Active/Muted/Paused) and `source` (default/override).
- A client's `activeContent` count === entries with state Active.
- For each content type, across the 9 clients:
  `clientsOnDefault + overrides === 9`.
- `pipelineDefaults` in `content.js` matches what the client `pipeline`
  arrays actually produce.
- `advisorPracticeProfile.clientCount === 9`.

If pipeline data changes, re-derive and verify equalities BEFORE committing.

### Client roster (Phase 1)

9 fictional athletes across 4 stages (`New` / `Active` / `Mature` / `Sunset`,
renameable per advisor preference): Marcus Thompson (canonical demo —
`c-001`), Jasmine Rivera, Reuben Asare, Ezekiel Banner, Isaiah Coleman,
Tariq Williams, Bree Caldwell, Naomi Pierce, Jordan Estes. Each carries
distinct fictional substance — different sports, gift sizes, giving
identities, session histories. **Never copy-paste Marcus's content under
another name.**

### Phase 1 is athletes only

No music / entertainment / creator language in user-facing copy. Sector
terminology is locked for later phases (athletics → music → entertainment →
creator, with role / event / completion / reinvestment-term substitutions)
but must not appear in current UI text. Non-athletics sectors use "Program
Reinvestment", not "endowment".

### Connection model (Individual surface)

Share / Connect / Signal Interest behave identically regardless of
subscription tier. Funders initiate; nonprofits don't push. Discovery is
for understanding, giving is for acting — **no donate button on the
discovery view**.

### StewardHouse never authors org-level content

Only structural elements (cause tags, fields, scaffolds). Org profile
narrative is org-authored or imported (Candid API integration is future).
Organizations are referenced entities, not platform users — the unclaimed
tier of the content-sourcing model; org records appear as discovery-catalog
entries, gift targets, and connection destinations, and the Operations
Organizations directory is the operator view of those records.

---

## 8. Key docs

- `docs/qa-audit-enterprise-2026-05-30.md` — 157-finding Enterprise audit (on
  `qa-audit-enterprise` branch; HEAD of audit). Exec-summary "Medium: 76" is a
  typo at source (actual 60); CLAUDE.md previously inherited the typo as
  "173 findings" — corrected during the Enterprise ground-truth investigation.
- `docs/qa-audit-operations-2026-06-09.md` — 56-finding Operations audit (on
  `qa-audit-operations` branch; HEAD includes the 3-finding recon amendment).
- `docs/qa-audit-advisor-2026-06-13.md` — 41-finding Advisor audit (on
  `qa-audit-advisor` branch; audit's exec-summary "43" is a typo, ADV-034 +
  ADV-037 never existed; +1 added during bundle 5 → denominator 42).
- `docs/operations-current-state-2026-05-31.md` — Operations surface inventory
  (pre-redesign baseline).
- `docs/operations-overview-discovery-2026-06-01.md` — gap-map between
  candidate Overview signals and live-versus-needs-new-entity classification
  (the foundation for slices A–H).
- `docs/cross-surface-data-model-discovery-2026-05-31.md` — data-shape
  mapping across all four surfaces (foundation for the unified data layer).
- `docs/qa-triage-medium-low-2026-05-30.md` — disposition of the 115 Enterprise
  Medium + Low audit findings.
- `docs/5.8-giving-flow-scoping.md` — Individual 5.8 giving-flow design rulings
  (FT, this session) + current-state baseline of the three gift shapes
  (seed / session / unified) + Parker hard lines + carried debt + next-turn
  placeholder for the canonical Gift field set + scenario-versioning spec.
- `docs/persistence-scoping-pass.md` — persistence scoping pass output (this
  session, read-only). Three strands — Strand 1 (D1 + Workers + unified
  architecture; live store = NEW layer beside unified; three open rulings),
  Strand 2 (`better-auth` self-hosted in D1 + magic-link via CF Email
  Service with Resend fallback; hosted SaaS rejected on custody grounds),
  Strand 3 (4/30 data-protection frame across 4 layers; Derek advanced to
  critical-path). Folded-item resolutions + what the schema draft must
  carry.
- `docs/persistence-schema-draft.md` — ruled persistence schema (FT, this
  session). Rule artifact in scope → rule → build. FT rulings A-F:
  A REPLACE (live D1 = single source of truth), B person-is-identity,
  C `person.id`-opaque sufficient, D `org` → D1 table, **E RESOLVED** on
  research synthesis (own doc below), F Marcus = first real account.
  Tables: `auth_user` (better-auth) / `person` / `gift`
  (`exported_to_cpa` = only lifecycle-adjacent field) / `scenario`
  (no rank / score) / `org`. Index every "my X" FK from migration 1.
  **Fully finalized**; FT has authorized build. Two counsel-gated seams
  remain isolated (Clause 3 charitable-floor; Clause 6 subpoena posture)
  and do NOT block build.
- `docs/ruling-e-deletion-retention.md` — ruling E (deletion / retention),
  accepted by FT this session on research synthesis. NOT legal advice;
  counsel-confirm/correct backstop. Six clauses: two-phase soft-then-hard
  deletion at the `person` boundary (Parker no-`deleted_at`-as-status
  invariant intact); anonymize-not-orphan; minimal justified retention
  window; backup-restore re-applies pending deletions; deletion ledger by
  opaque marker; subpoena posture keeps identity separable + who-gave-to-
  whom view UNBUILT. **Two counsel-gated seams isolated** — Clause 3
  charitable-retention-floor confirmation, Clause 6 subpoena posture —
  isolated, do NOT block the build; reviewing counsel confirms in
  parallel.
- `src/data/unified/README.md` — unified data layer internal notes.

---

## When in doubt

Ask. Don't assume. Path B violations, brand-token deviations, and Co-Authored-By
footers are non-negotiable.
